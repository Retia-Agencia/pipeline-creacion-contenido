{
  "name": "Descubrimiento de referentes — sugeridos IG + TikTok → propuestos (ADR-020)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "<<WEBHOOK_PATH_DESCUBRIMIENTO>>",
        "responseMode": "onReceived",
        "options": {},
        "authentication": "headerAuth"
      },
      "id": "disparo-descubrimiento-webhook",
      "name": "Buscar ahora (webhook)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        240,
        380
      ],
      "credentials": {
        "httpHeaderAuth": {
          "name": "Webhook Descubrimiento Header"
        }
      }
    },
    {
      "parameters": {},
      "name": "Ejecutar manual",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "id": "ejecutar-manual",
      "position": [
        450,
        550
      ]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "c2",
              "name": "supabase_url",
              "value": "<<SUPABASE_URL>>",
              "type": "string"
            },
            {
              "id": "c_dash",
              "name": "dashboard_url",
              "value": "<<DASHBOARD_URL>>",
              "type": "string"
            },
            {
              "id": "c3",
              "name": "instance_id",
              "value": "={{ $('Buscar ahora (webhook)').isExecuted ? (($('Buscar ahora (webhook)').first().json.body || {}).instancia || '') : '' }}",
              "type": "string"
            },
            {
              "id": "c4",
              "name": "cap_semillas",
              "value": 8,
              "type": "number"
            },
            {
              "id": "c5",
              "name": "cap_perfiles_detalle",
              "value": 20,
              "type": "number"
            },
            {
              "id": "c6",
              "name": "propuestas_max",
              "value": 10,
              "type": "number"
            },
            {
              "id": "c7",
              "name": "afinidad_minima",
              "value": 0.6,
              "type": "number"
            },
            {
              "id": "c8",
              "name": "cap_lookalikes_tt",
              "value": 15,
              "type": "number"
            },
            {
              "id": "c9",
              "name": "descubrir_ig",
              "value": 1,
              "type": "number"
            },
            {
              "id": "c10",
              "name": "descubrir_tt",
              "value": 1,
              "type": "number"
            }
          ]
        },
        "options": {}
      },
      "name": "Config",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "id": "config",
      "position": [
        660,
        380
      ],
      "notes": "instance_id sale del payload del webhook ({ instancia }), no de un placeholder (ADR-048).\nPara «Ejecutar manual»: reemplazá el '' final de la expresión por el uuid de la instancia.\nEn git va VACÍO a propósito — vacío hace que run-plan responda 400 y la corrida no arranque.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/runs' }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=representation"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={ \"instance_id\": \"{{ $('Config').first().json.instance_id }}\", \"trigger_type\": \"{{ $('Ejecutar manual').isExecuted ? 'manual' : 'on_demand' }}\", \"estado\": \"en_curso\", \"params\": { \"workflow\": \"descubrimiento\", \"execution_id\": \"{{ $execution.id }}\" } }",
        "options": {
          "timeout": 15000
        }
      },
      "name": "Abrir run en el registro",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput",
      "id": "abrir-run-en-el-registro",
      "position": [
        870,
        380
      ]
    },
    {
      "parameters": {
        "method": "PATCH",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/runs?params-%3E%3Eworkflow=eq.descubrimiento&instance_id=eq.' + $('Config').first().json.instance_id + '&estado=eq.en_curso&id=neq.' + ($('Abrir run en el registro').first().json.id || '00000000-0000-0000-0000-000000000000') }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { estado: 'fallo', fin: $now.toISO(), error: 'run de descubrimiento sin cerrar (fallo antes de Cerrar run); barrido por corrida posterior' } }}",
        "options": {
          "timeout": 20000
        }
      },
      "name": "Barrer runs zombie",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput",
      "id": "barrer-runs-zombie",
      "position": [
        1080,
        380
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/referentes_propuestos?select=handle,plataforma&instance_id=eq.' + $('Config').first().json.instance_id }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept-Profile",
              "value": "app"
            }
          ]
        },
        "options": {
          "timeout": 20000
        }
      },
      "name": "Leer Propuestos",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput",
      "id": "leer-propuestos",
      "position": [
        1920,
        380
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/v_senal_seleccion?select=referente,tasa_seleccion,calificados&instance_id=eq.' + $('Config').first().json.instance_id }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "options": {
          "timeout": 15000
        }
      },
      "name": "Leer señal selección",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput",
      "id": "leer-senal-seleccion",
      "position": [
        660,
        720
      ]
    },
    {
      "parameters": {
        "jsCode": "// Armar plan de descubrimiento (ADR-020) — semillas rankeadas por señal + set de dedup + knobs.\nconst recs = (n) => $(n).all().flatMap((it) => (it.json && it.json.records) || []);\nconst _nrm = (s) => (s || '').toString().toLowerCase().normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').replace(/\\s+/g, ' ').trim();\nconst cfg0 = $('Config').first().json;\n// Knobs del equipo (Ajustes) sobre defaults de Config; caps de costo solo en Config (patrón ADR-011/016).\nconst AJUSTE_MAP = { 'propuestas por corrida': 'propuestas_max', 'afinidad minima de propuesta': 'afinidad_minima', 'descubrir en instagram': 'descubrir_ig', 'descubrir en tiktok': 'descubrir_tt' };\nconst plan = $('Leer plan (fachada)').first().json;\nconst ajustes = {};\n(plan.ajustes || []).forEach(a => { const f = a.fields || {}; const k = AJUSTE_MAP[_nrm(f.clave)]; if (k && f.valor !== undefined && f.valor !== null && f.valor !== '') ajustes[k] = Number(f.valor); });\nconst pick = (k, d) => (ajustes[k] != null ? ajustes[k] : (cfg0[k] != null ? Number(cfg0[k]) : d));\nconst propuestas_max = Math.max(1, Number(pick('propuestas_max', 10)));\nconst afinidad_minima = Number(pick('afinidad_minima', 0.6));\nconst cap_semillas = Math.max(1, Number(cfg0.cap_semillas || 8));\nconst cap_detalle = Math.max(1, Number(cfg0.cap_perfiles_detalle || 20));\n// Toggles de eje del descubrimiento (equipo, Ajustes > Config; default ON). Off = ese eje no genera semillas.\nconst DESCUBRIR_IG = Number(pick('descubrir_ig', 1)) > 0;\nconst DESCUBRIR_TT = Number(pick('descubrir_tt', 1)) > 0;\n\n// Proyectos activos con criterios Proyecto ⊕ Voz (mismo modelo que el gate del motor, ADR-010).\n// ADR-079: la voz entra con su `activo`, no solo con sus criterios — es el gate de abajo.\nconst voces = {};\n(plan.voces || []).forEach(v => { const f = v.fields || {}; voces[v.id] = { criterios: (f.criterios_relevancia || '').toString().trim(), activo: !!f.activo }; });\nconst projects = {};\n(plan.proyectos || []).forEach(p => { const f = p.fields || {};\n  if (!f.activo) return; // ambito=completo no filtra: la regla vive acá, explicita\n  const vozId = Array.isArray(f.voz_default) && f.voz_default[0] ? f.voz_default[0] : null;\n  // ADR-079: misma regla que `Armar plan de corrida` — proyecto activo de VOZ APAGADA no entra.\n  // Un proyecto SIN voz no esta gateado (entra), igual que en el motor. `ambito=completo` no\n  // filtra voces a proposito: el descubrimiento necesita los referentes inactivos para dedup.\n  if (vozId && !(voces[vozId] && voces[vozId].activo)) { console.log('[Descubrimiento] proyecto salteado (voz apagada): ' + (f.nombre || p.id)); return; }\n  projects[p.id] = { id: p.id, nombre: f.nombre || '', criterios: (f.criterios_relevancia || '').toString().trim(), voz_criterios: (vozId && voces[vozId] && voces[vozId].criterios) || '' };\n});\n\n// Dedup: cualquier handle ya conocido no se re-propone (Referentes activo o no + Propuestos en cualquier estado).\n// Dedup por (plataforma, handle): TikTok e Instagram son namespaces distintos, no se cruzan.\nconst conocidos_ig = new Set(); const conocidos_tt = new Set();\nconst referentes = plan.referentes || [];\nconst _dedup = (r) => { const f = r.fields || {}; const h = String(f.handle || '').trim().replace(/^@+/, '').toLowerCase(); if (!h) return; (String(f.plataforma || '').toLowerCase().indexOf('tik') >= 0 ? conocidos_tt : conocidos_ig).add(h); };\nreferentes.forEach(_dedup);\n// D7: `Leer Propuestos` ahora es PostgREST, que devuelve filas PLANAS (no {records:[{fields}]}).\nconst _filas = (n) => { try { const a = $(n).all().map(i => i.json); return (a.length === 1 && Array.isArray(a[0])) ? a[0] : a; } catch (e) { return []; } };\n_filas('Leer Propuestos').forEach(f => _dedup({ fields: f }));\nconst conocidos = new Set([...conocidos_ig, ...conocidos_tt]);\n\n// Semillas: referentes IG activos de proyectos activos, rankeados por tasa de selección (fail-open sin señal).\nconst senal = {};\n$('Leer señal selección').all().forEach(it => { const s = it.json || {}; if (s.referente) senal[String(s.referente).toLowerCase()] = { tasa: Number(s.tasa_seleccion || 0), cal: Number(s.calificados || 0) }; });\nconst semMap = {};\nreferentes.forEach(r => { const f = r.fields || {};\n  if (!f.activo) return;\n  if (String(f.plataforma || '').toLowerCase().indexOf('insta') < 0) return; // v1 solo IG (ADR-020)\n  const h = String(f.handle || '').trim().replace(/^@+/, ''); if (!h) return;\n  const proys = (Array.isArray(f.proyecto) ? f.proyecto : []).filter(id => projects[id]); if (!proys.length) return;\n  const k = h.toLowerCase();\n  (semMap[k] = semMap[k] || { handle: h, proyectos: new Set() });\n  proys.forEach(id => semMap[k].proyectos.add(id));\n});\nconst semillas = (DESCUBRIR_IG ? Object.entries(semMap) : [])\n  .map(([k, v]) => ({ handle: v.handle, proyectos: [...v.proyectos], tasa: (senal[k] || {}).tasa || 0, cal: (senal[k] || {}).cal || 0 }))\n  .sort((a, b) => (b.tasa - a.tasa) || (b.cal - a.cal))\n  .slice(0, cap_semillas);\nconst seed_to_proj = {};\nsemillas.forEach(s => { seed_to_proj[s.handle.toLowerCase()] = s.proyectos; });\nconsole.log('[Descubrimiento] semillas IG (' + semillas.length + '): ' + semillas.map(s => s.handle + '(' + s.tasa.toFixed(2) + ')').join(', '));\n\n// Semillas TikTok (ADR-020 extendido): referentes TT activos, rankeados por señal. El lookalike actor\n// (dataovercoffee) mezcla todas las semillas y devuelve un ranking global con score; la atribución a\n// proyecto la resuelve el vetting Haiku contra los criterios de cada proyecto con semillas TT.\nconst semMapTT = {};\nreferentes.forEach(r => { const f = r.fields || {};\n  if (!f.activo) return;\n  if (String(f.plataforma || '').toLowerCase().indexOf('tik') < 0) return;\n  const h = String(f.handle || '').trim().replace(/^@+/, ''); if (!h) return;\n  const proys = (Array.isArray(f.proyecto) ? f.proyecto : []).filter(id => projects[id]); if (!proys.length) return;\n  const k = h.toLowerCase();\n  (semMapTT[k] = semMapTT[k] || { handle: h, proyectos: new Set() });\n  proys.forEach(id => semMapTT[k].proyectos.add(id));\n});\nconst semillasTT = (DESCUBRIR_TT ? Object.entries(semMapTT) : [])\n  .map(([k, v]) => ({ handle: v.handle, proyectos: [...v.proyectos], tasa: (senal[k] || {}).tasa || 0, cal: (senal[k] || {}).cal || 0 }))\n  .sort((a, b) => (b.tasa - a.tasa) || (b.cal - a.cal))\n  .slice(0, cap_semillas);\nconst seed_to_proj_tt = {}; const ttProjIds = new Set();\nsemillasTT.forEach(s => { seed_to_proj_tt[s.handle.toLowerCase()] = s.proyectos; s.proyectos.forEach(p => ttProjIds.add(p)); });\nconst cap_detalle_tt = Math.max(1, Number(cfg0.cap_lookalikes_tt || 15));\nconsole.log('[Descubrimiento] semillas TT (' + semillasTT.length + '): ' + semillasTT.map(s => s.handle).join(', '));\n\nreturn [{ json: { semillas: semillas.map(s => s.handle), seed_to_proj, conocidos: [...conocidos], conocidos_ig: [...conocidos_ig], conocidos_tt: [...conocidos_tt], projects, propuestas_max, afinidad_minima, cap_detalle, tt_semillas: semillasTT.map(s => s.handle), seed_to_proj_tt, tt_project_ids: [...ttProjIds], cap_detalle_tt } }];"
      },
      "name": "Armar plan de descubrimiento",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "armar-plan-de-descubrimiento",
      "position": [
        870,
        720
      ]
    },
    {
      "parameters": {
        "authentication": "apifyApi",
        "resource": "Actors",
        "operation": "Run actor and get dataset",
        "actorId": "apify~instagram-profile-scraper",
        "customBody": "={{ { usernames: $('Armar plan de descubrimiento').first().json.semillas } }}"
      },
      "name": "Apify — Perfiles semilla",
      "type": "@apify/n8n-nodes-apify.apify",
      "typeVersion": 1,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "id": "apify-perfiles-semilla",
      "position": [
        1080,
        720
      ]
    },
    {
      "parameters": {
        "jsCode": "// Agregar sugeridos — junta relatedProfiles de todas las semillas, dedup y rankea por frecuencia\n// (sugerido por N semillas > sugerido por 1). Corta a cap_perfiles_detalle antes de pagar el detalle.\nconst plan = $('Armar plan de descubrimiento').first().json;\nconst conocidos = new Set(plan.conocidos_ig || plan.conocidos || []);\nconst seedToProj = plan.seed_to_proj || {};\nconst cand = {};\nfor (const it of $input.all()) {\n  const d = it.json || {};\n  const seed = String(d.username || '').toLowerCase();\n  const proys = seedToProj[seed] || [];\n  for (const rp of (Array.isArray(d.relatedProfiles) ? d.relatedProfiles : [])) {\n    const u = String((rp && rp.username) || '').trim().replace(/^@+/, ''); if (!u) continue;\n    if (rp.is_private) continue;\n    const k = u.toLowerCase(); if (conocidos.has(k)) continue;\n    const c = (cand[k] = cand[k] || { username: u, freq: 0, proyectos: new Set(), semillas: new Set() });\n    c.freq++; proys.forEach(p => c.proyectos.add(p)); if (seed) c.semillas.add(seed);\n  }\n}\nconst top = Object.values(cand).sort((a, b) => b.freq - a.freq).slice(0, Number(plan.cap_detalle || 20));\nconst candidatos = {};\ntop.forEach(c => { candidatos[c.username.toLowerCase()] = { username: c.username, freq: c.freq, proyectos: [...c.proyectos], semillas: [...c.semillas] }; });\nconsole.log('[Descubrimiento] sugeridos únicos nuevos: ' + Object.keys(cand).length + ' → pasan a detalle: ' + top.length);\nreturn [{ json: { usernames: top.map(c => c.username), candidatos, unicos: Object.keys(cand).length } }];"
      },
      "name": "Agregar sugeridos",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "agregar-sugeridos",
      "position": [
        1290,
        720
      ]
    },
    {
      "parameters": {
        "authentication": "apifyApi",
        "resource": "Actors",
        "operation": "Run actor and get dataset",
        "actorId": "apify~instagram-profile-scraper",
        "customBody": "={{ { usernames: $('Agregar sugeridos').first().json.usernames } }}"
      },
      "name": "Apify — Detalle sugeridos",
      "type": "@apify/n8n-nodes-apify.apify",
      "typeVersion": 1,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "id": "apify-detalle-sugeridos",
      "position": [
        1500,
        720
      ]
    },
    {
      "parameters": {
        "jsCode": "// Vetting relevancia (Haiku) — jurado estricto sobre bio + captions de últimos posts, por proyecto\n// reclamante (criterios Proyecto ⊕ Voz, ADR-010). FAIL-CLOSED (ADR-020): si Haiku falla o no hay\n// criterios, NO se propone (el riesgo acá es inundar al equipo de ruido, no perder contenido).\nconst KEY = '<ANTHROPIC_API_KEY>';\nconst plan = $('Armar plan de descubrimiento').first().json;\nconst projects = plan.projects || {};\nconst candidatos = ($('Agregar sugeridos').first().json || {}).candidatos || {};\nconst sleep = (ms) => new Promise(r => setTimeout(r, ms));\n\nconst detalle = {};\nfor (const it of $input.all()) {\n  const d = it.json || {};\n  const u = String(d.username || '').toLowerCase(); if (!u || !candidatos[u]) continue;\n  if (d.private) continue;\n  const caps = (Array.isArray(d.latestPosts) ? d.latestPosts : [])\n    .map(p => String((p && p.caption) || '').replace(/\\s+/g, ' ').trim()).filter(Boolean)\n    .slice(0, 8).map(c => c.slice(0, 180));\n  detalle[u] = { username: d.username, bio: String(d.biography || '').trim(), seguidores: Number(d.followersCount || 0), caps, url: String(d.url || ('https://www.instagram.com/' + d.username + '/')) };\n}\n\nconst porProy = {};\nObject.values(candidatos).forEach(c => { const u = c.username.toLowerCase(); if (!detalle[u]) return;\n  c.proyectos.forEach(pid => { if (!projects[pid]) return; (porProy[pid] = porProy[pid] || []).push(u); });\n});\n\nconst juicio = {}; // username → { afin, razon, proyectos }\nlet first = true;\nfor (const pid in porProy) {\n  const p = projects[pid];\n  const criterios = [p.criterios ? 'CRITERIOS DE TEMA (principal):\\n' + p.criterios : '', p.voz_criterios ? 'AJUSTE DE VOZ (complementa, no manda):\\n' + p.voz_criterios : ''].filter(Boolean).join('\\n\\n');\n  if (!criterios) { console.log('[Vetting] proyecto sin criterios, se salta (fail-closed): ' + p.nombre); continue; }\n  const lote = porProy[pid].map(u => { const d = detalle[u]; return { id: d.username, bio: d.bio.slice(0, 300), posts: d.caps.join(' | ').slice(0, 1200), seguidores: d.seguidores }; });\n  if (!lote.length) continue;\n  if (!first) await sleep(1000); first = false;\n  const sys = 'Sos un jurado ESTRICTO que evalua CUENTAS de Instagram como posibles referentes de contenido para un proyecto. Te doy el tema del proyecto, sus criterios, y una lista de cuentas (id + bio + captions de sus ultimos posts). Para cada cuenta decidi que tan AFIN es el contenido que publica a los criterios (importa lo que publica, no su fama). Precision: ante la duda, score bajo. Devolve UNICAMENTE un JSON {\"juicio\":[{\"id\":\"...\",\"afin\":0.0,\"razon\":\"breve, en espanol\"}]}. afin 0..1.';\n  const usr = 'TEMA: ' + (p.nombre || '') + '\\n' + criterios + '\\n\\nCUENTAS:\\n' + JSON.stringify(lote);\n  try {\n    const resp = await this.helpers.httpRequest({\n      method: 'POST', url: 'https://api.anthropic.com/v1/messages',\n      headers: { 'x-api-key': KEY, 'anthropic-version': '2023-06-01', 'content-type': 'application/json' },\n      body: { model: 'claude-haiku-4-5', max_tokens: 4000, system: sys, messages: [{ role: 'user', content: usr }] },\n      json: true, timeout: 60000\n    });\n    let txt = ''; (resp.content || []).forEach(b => { if (b && b.type === 'text') txt += b.text; });\n    const m = txt.match(/\\{[\\s\\S]*\\}/);\n    const j = m ? JSON.parse(m[0]) : null;\n    ((j && j.juicio) || []).forEach(x => {\n      const u = String(x.id || '').toLowerCase(); if (!detalle[u]) return;\n      const afin = Math.max(0, Math.min(1, Number(x.afin || 0)));\n      const prev = juicio[u];\n      if (!prev) juicio[u] = { afin, razon: String(x.razon || ''), proyectos: [pid] };\n      else { if (afin > prev.afin) { prev.afin = afin; prev.razon = String(x.razon || ''); } if (prev.proyectos.indexOf(pid) < 0) prev.proyectos.push(pid); }\n    });\n  } catch (e) { console.log('[Vetting] Haiku falló para ' + p.nombre + ' — FAIL-CLOSED, no se propone nada de este lote: ' + (e.message || e)); }\n}\n\nconst out = [];\nfor (const u in juicio) { const d = detalle[u]; const c = candidatos[u];\n  out.push({ json: { username: d.username, afin: juicio[u].afin, razon: juicio[u].razon, proyectos: juicio[u].proyectos, seguidores: d.seguidores, bio: d.bio, url: d.url, semillas: (c.semillas || []).join(', '), freq: c.freq || 1 } });\n}\nconsole.log('[Vetting] cuentas juzgadas con score: ' + out.length);\nif (!out.length) return [{ json: { _vacio: true } }]; // la cadena sigue hasta Cerrar run\nreturn out;"
      },
      "name": "Vetting relevancia (Haiku)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "vetting-relevancia-haiku",
      "position": [
        1710,
        720
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ ($('Armar plan de descubrimiento').first().json.tt_semillas || []).length }}",
              "rightValue": 0,
              "operator": {
                "type": "number",
                "operation": "gt"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "name": "IF — hay semillas TT",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "id": "if-hay-semillas-tt",
      "position": [
        1290,
        900
      ]
    },
    {
      "parameters": {
        "authentication": "apifyApi",
        "resource": "Actors",
        "operation": "Run actor and get dataset",
        "actorId": "dataovercoffee~tiktok-lookalike-search",
        "customBody": "={{ { seed_usernames: $('Armar plan de descubrimiento').first().json.tt_semillas, exclude_usernames: $('Armar plan de descubrimiento').first().json.conocidos_tt, limit: $('Armar plan de descubrimiento').first().json.cap_detalle_tt } }}"
      },
      "name": "Apify — Lookalikes TikTok",
      "type": "@apify/n8n-nodes-apify.apify",
      "typeVersion": 1,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "id": "apify-lookalikes-tiktok",
      "position": [
        1500,
        900
      ]
    },
    {
      "parameters": {
        "jsCode": "// Vetting TikTok (Haiku) — jurado estricto sobre bio + métricas (el lookalike actor no expone captions),\n// por proyecto con semillas TT (criterios Proyecto ⊕ Voz, ADR-010). FAIL-CLOSED como IG (ADR-020):\n// solo adjunta el proyecto donde la afinidad supera el umbral, así la sugerencia lleva el proyecto correcto.\nconst KEY = '<ANTHROPIC_API_KEY>';\nconst plan = $('Armar plan de descubrimiento').first().json;\nconst projects = plan.projects || {};\nconst ttProjectIds = plan.tt_project_ids || [];\nconst MIN = Number(plan.afinidad_minima || 0.6);\nconst conocidosTT = new Set(plan.conocidos_tt || []);\nconst semillasTxt = (plan.tt_semillas || []).join(', ');\nconst sleep = (ms) => new Promise(r => setTimeout(r, ms));\n\nconst detalle = {};\nfor (const it of $input.all()) {\n  const d = it.json || {};\n  const u = String(d.unique_id || '').trim().replace(/^@+/, ''); if (!u) continue;\n  const k = u.toLowerCase(); if (conocidosTT.has(k) || detalle[k]) continue;\n  detalle[k] = { username: u, bio: String(d.signature || '').trim(), seguidores: Number(d.follower_count || 0), score: Number(d.score || 0), url: String(d.tiktok_url || ('https://www.tiktok.com/@' + u)), avg_views: Number(d.recent_avg_play_count || 0), avg_likes: Number(d.recent_avg_like_count || 0) };\n}\nconst lista = Object.values(detalle);\nif (!lista.length) { console.log('[Vetting TT] 0 lookalikes'); return [{ json: { _vacio: true } }]; }\n\nconst juicio = {}; // username → { afin, razon, proyectos }\nlet firstCall = true;\nfor (const pid of ttProjectIds) {\n  const p = projects[pid]; if (!p) continue;\n  const criterios = [p.criterios ? 'CRITERIOS DE TEMA (principal):\\n' + p.criterios : '', p.voz_criterios ? 'AJUSTE DE VOZ (complementa, no manda):\\n' + p.voz_criterios : ''].filter(Boolean).join('\\n\\n');\n  if (!criterios) { console.log('[Vetting TT] proyecto sin criterios, se salta (fail-closed): ' + p.nombre); continue; }\n  const lote = lista.map(d => ({ id: d.username, bio: d.bio.slice(0, 300), seguidores: d.seguidores, avg_views: d.avg_views, avg_likes: d.avg_likes }));\n  if (!firstCall) await sleep(1000); firstCall = false;\n  const sys = 'Sos un jurado ESTRICTO que evalua CUENTAS de TikTok como posibles referentes de contenido para un proyecto. Te doy el tema del proyecto, sus criterios, y una lista de cuentas (id + bio + metricas: seguidores, vistas y likes promedio). OJO: no tenes el texto de los videos, solo bio y metricas, asi que se conservador. Para cada cuenta decidi que tan AFIN es el contenido a los criterios (importa el tema, no la fama). Ante la duda, score bajo. Devolve UNICAMENTE un JSON {\"juicio\":[{\"id\":\"...\",\"afin\":0.0,\"razon\":\"breve, en espanol\"}]}. afin 0..1.';\n  const usr = 'TEMA: ' + (p.nombre || '') + '\\n' + criterios + '\\n\\nCUENTAS:\\n' + JSON.stringify(lote);\n  try {\n    const resp = await this.helpers.httpRequest({\n      method: 'POST', url: 'https://api.anthropic.com/v1/messages',\n      headers: { 'x-api-key': KEY, 'anthropic-version': '2023-06-01', 'content-type': 'application/json' },\n      body: { model: 'claude-haiku-4-5', max_tokens: 4000, system: sys, messages: [{ role: 'user', content: usr }] },\n      json: true, timeout: 60000\n    });\n    let txt = ''; (resp.content || []).forEach(b => { if (b && b.type === 'text') txt += b.text; });\n    const m = txt.match(/\\{[\\s\\S]*\\}/);\n    const j = m ? JSON.parse(m[0]) : null;\n    ((j && j.juicio) || []).forEach(x => {\n      const u = String(x.id || '').toLowerCase(); if (!detalle[u]) return;\n      const afin = Math.max(0, Math.min(1, Number(x.afin || 0)));\n      if (afin < MIN) return; // solo se adjunta el proyecto donde la cuenta es relevante\n      const prev = juicio[u];\n      if (!prev) juicio[u] = { afin, razon: String(x.razon || ''), proyectos: [pid] };\n      else { if (afin > prev.afin) { prev.afin = afin; prev.razon = String(x.razon || ''); } if (prev.proyectos.indexOf(pid) < 0) prev.proyectos.push(pid); }\n    });\n  } catch (e) { console.log('[Vetting TT] Haiku falló para ' + p.nombre + ' — FAIL-CLOSED: ' + (e.message || e)); }\n}\n\nconst out = [];\nfor (const u in juicio) { const d = detalle[u];\n  out.push({ json: { username: d.username, plataforma: 'tiktok', afin: juicio[u].afin, razon: juicio[u].razon, proyectos: juicio[u].proyectos, seguidores: d.seguidores, bio: d.bio, url: d.url, semillas: semillasTxt, freq: Math.round((d.score || 0) * 100) } });\n}\nconsole.log('[Vetting TT] cuentas afines: ' + out.length);\nif (!out.length) return [{ json: { _vacio: true } }]; // la cadena sigue hasta Armar propuestas\nreturn out;"
      },
      "name": "Vetting TikTok (Haiku)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "id": "vetting-tiktok-haiku",
      "position": [
        1710,
        900
      ]
    },
    {
      "parameters": {
        "jsCode": "// Armar propuestas — junta IG + TikTok (leídos por nombre, cada fila trae su plataforma y proyecto),\n// umbral de afinidad + cap por corrida (Ajustes) → filas planas para PostgREST (ADR-035).\n//\n// D7 cambió dos cosas acá:\n//  1. Se escribe en Postgres, así que no hay `fields`, ni `typecast`, ni batches de 10: PostgREST\n//     acepta el array entero de una.\n//  2. **Los proyectos viajan aparte, como PARES.** `app.referentes_propuestos` no tiene columna\n//     `proyecto_id`: la relación es N:M (enmienda de ADR-032 — medido antes del corte, las 8\n//     propuestas vivas tenían 2 proyectos cada una, así que una FK simple tiraba la mitad).\nconst plan = $('Armar plan de descubrimiento').first().json;\nconst MIN = Number(plan.afinidad_minima || 0.6);\nconst MAX = Math.max(1, Number(plan.propuestas_max || 10));\nconst grab = (n) => { try { return $(n).all().map(i => i.json); } catch (e) { return []; } };\nconst items = [...grab('Vetting relevancia (Haiku)'), ...grab('Vetting TikTok (Haiku)')].filter(d => d && d.username && !d._vacio);\nconst elegidos = items.filter(d => Number(d.afin || 0) >= MIN)\n  .sort((a, b) => (b.afin - a.afin) || ((b.freq || 0) - (a.freq || 0))).slice(0, MAX);\nif (!elegidos.length) { console.log('[Descubrimiento] 0 propuestas sobre el umbral ' + MIN); return [{ json: { hay_propuestas: false, propuestos: 0 } }]; }\n\n// Los ids del plan YA son los uuid de Postgres: el mapa `uuidDe` murió con el contrato v2\n// (ADR-048 §5), donde `fields.uuid` dejó de viajar. Los proyectos conocidos son los del plan, y\n// un id que no esté ahí se saltea en vez de romper la corrida entera.\nconst conocidos = new Set((($('Leer plan (fachada)').first().json || {}).proyectos || []).map(p => p.id));\n\nconst filas = elegidos.map(d => ({\n  handle: '@' + d.username, plataforma: d.plataforma || 'instagram',\n  afinidad: Math.round(Number(d.afin) * 100) / 100, razon: String(d.razon || '').slice(0, 500),\n  seguidores: Number(d.seguidores || 0), bio: String(d.bio || '').slice(0, 500), url: d.url,\n  semillas: d.semillas || '', estado: 'propuesto',\n  instance_id: $('Config').first().json.instance_id\n}));\n// Los pares se llevan por handle porque el uuid de la propuesta todavía no existe: lo acuña el\n// insert. `Preparar puente propuestas` los cruza contra lo que PostgREST devuelve.\nconst pares = elegidos.map((d, i) => ({\n  handle: filas[i].handle.toLowerCase(),\n  proyecto_ids: (d.proyectos || []).filter(id => conocidos.has(id))\n}));\nconst _sinUuid = elegidos.reduce((a, d) => a + (d.proyectos || []).filter(id => !conocidos.has(id)).length, 0);\nif (_sinUuid) console.log('[Descubrimiento] ⚠️ ' + _sinUuid + ' vínculo(s) a proyectos que no están en el plan: se saltean');\nconsole.log('[Descubrimiento] proponiendo ' + filas.length + ' con ' + pares.reduce((a, p) => a + p.proyecto_ids.length, 0) + ' pares: ' + elegidos.map(d => d.username + '(' + (d.plataforma || 'ig') + ' ' + d.afin + ')').join(', '));\nreturn [{ json: { hay_propuestas: true, filas, pares, propuestos: filas.length } }];"
      },
      "name": "Armar propuestas",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "armar-propuestas",
      "position": [
        1920,
        720
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ ($json.filas || []).length }}",
              "rightValue": 0,
              "operator": {
                "type": "number",
                "operation": "gt"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "name": "IF — hay propuestas",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "id": "if-hay-propuestas",
      "position": [
        2130,
        720
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/referentes_propuestos' }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Content-Profile",
              "value": "app"
            },
            {
              "name": "Prefer",
              "value": "return=representation"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.filas) }}",
        "options": {
          "timeout": 20000
        }
      },
      "name": "POST Propuestos",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "id": "post-propuestos-pg",
      "position": [
        2340,
        720
      ]
    },
    {
      "parameters": {
        "jsCode": "// Preparar puente propuestas (D7) — cruza lo que PostgREST acaba de crear contra los pares que\n// armó `Armar propuestas`, para llenar `app.referentes_propuestos_proyectos`.\n//\n// Por qué hace falta este paso y no se pudo escribir todo de una: el uuid de la propuesta lo\n// acuña el insert, así que los vínculos no se pueden armar antes de tenerlo. Se cruza por\n// **handle** y no por posición: PostgREST conserva el orden, pero depender de eso vuelve el\n// vínculo frágil ante cualquier cambio futuro, y acá un cruce mal hecho no falla — enlaza una\n// propuesta al proyecto equivocado.\nconst _filas = (n) => { try { const a = $(n).all().map(i => i.json); return (a.length === 1 && Array.isArray(a[0])) ? a[0] : a; } catch (e) { return []; } };\nconst creadas = _filas('POST Propuestos').filter(r => r && r.id && r.handle);\nconst idDe = {};\ncreadas.forEach(r => { idDe[String(r.handle).toLowerCase()] = r.id; });\n\nconst pares = [];\nfor (const p of ($('Armar propuestas').first().json.pares || [])) {\n  const propuestoId = idDe[p.handle];\n  if (!propuestoId) continue;\n  for (const proyectoId of (p.proyecto_ids || [])) pares.push({ propuesto_id: propuestoId, proyecto_id: proyectoId });\n}\nconsole.log('[Descubrimiento] puente: ' + pares.length + ' pares para ' + creadas.length + ' propuestas');\n// Sin pares no hay nada que postear, y PostgREST con [] devuelve 400: se corta acá.\nif (!pares.length) return [];\nreturn [{ json: { pares } }];"
      },
      "id": "preparar-puente-propuestas",
      "name": "Preparar puente propuestas",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2560,
        720
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/referentes_propuestos_proyectos' }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Content-Profile",
              "value": "app"
            },
            {
              "name": "Prefer",
              "value": "resolution=ignore-duplicates"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.pares) }}",
        "options": {
          "timeout": 20000
        }
      },
      "id": "post-puente-propuestas",
      "name": "POST Puente propuestas",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2780,
        720
      ],
      "onError": "continueRegularOutput",
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "method": "PATCH",
        "url": "={{ $('Config').first().json.supabase_url + '/rest/v1/runs?id=eq.' + ($('Abrir run en el registro').first().json.id || '00000000-0000-0000-0000-000000000000') }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ (function(){\nvar g = function(n, f){ try { return f($(n)); } catch (e) { return null; } };\nvar plan = g('Armar plan de descubrimiento', function(x){ return x.first().json; }) || {};\nvar ag = g('Agregar sugeridos', function(x){ return x.first().json; }) || {};\nvar vet = g('Vetting relevancia (Haiku)', function(x){ return x.all().map(function(i){ return i.json; }).filter(function(d){ return d && d.username; }); }) || [];\nvar vetTT = g('Vetting TikTok (Haiku)', function(x){ return x.all().map(function(i){ return i.json; }).filter(function(d){ return d && d.username; }); }) || [];\nvar look = g('Apify — Lookalikes TikTok', function(x){ return x.all().map(function(i){ return i.json; }).filter(function(d){ return d && d.unique_id; }); }) || [];\nvar ap = g('Armar propuestas', function(x){ return x.first().json; }) || {};\nreturn { fin: $now.toISO(), estado: 'ok', metricas: { semillas: (plan.semillas || []).length + (plan.tt_semillas || []).length, sugeridos_unicos: (ag.unicos || 0) + look.length, detalle: (ag.usernames || []).length + look.length, vetteados: vet.length + vetTT.length, propuestos: ap.propuestos || 0, perfiles_semilla: g('Apify — Perfiles semilla', function(x){ return x.all().length; }) || 0, detalle_sugeridos: g('Apify — Detalle sugeridos', function(x){ return x.all().length; }) || 0, lookalikes_tt: g('Apify — Lookalikes TikTok', function(x){ return x.all().length; }) || 0 } };\n})() }}",
        "options": {
          "timeout": 15000
        }
      },
      "name": "Cerrar run en el registro",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "supabaseApi": {
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput",
      "id": "cerrar-run-en-el-registro",
      "position": [
        2550,
        720
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').first().json.dashboard_url + '/api/engine/run-plan?ambito=completo&instancia=' + $('Config').first().json.instance_id }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "httpHeaderAuth",
        "options": {
          "timeout": 30000
        }
      },
      "id": "leer-plan-fachada-desc",
      "name": "Leer plan (fachada)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1290,
        380
      ],
      "credentials": {
        "httpHeaderAuth": {
          "name": "Run Plan Header"
        }
      },
      "executeOnce": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "notes": "La config del cockpit (ADR-028), ambito=completo: SIN filtros de activo, el total tal cual, porque este workflow aplica su propia logica encima. FAIL-CLOSED: sin onError, !=200 tras 3 intentos aborta el run. execute-once: lookup de corrida, no de item (cierre 67)."
    }
  ],
  "pinData": {},
  "connections": {
    "Ejecutar manual": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Abrir run en el registro",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Abrir run en el registro": {
      "main": [
        [
          {
            "node": "Barrer runs zombie",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Barrer runs zombie": {
      "main": [
        [
          {
            "node": "Leer plan (fachada)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Leer Propuestos": {
      "main": [
        [
          {
            "node": "Leer señal selección",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Leer señal selección": {
      "main": [
        [
          {
            "node": "Armar plan de descubrimiento",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Armar plan de descubrimiento": {
      "main": [
        [
          {
            "node": "Apify — Perfiles semilla",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apify — Perfiles semilla": {
      "main": [
        [
          {
            "node": "Agregar sugeridos",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Agregar sugeridos": {
      "main": [
        [
          {
            "node": "Apify — Detalle sugeridos",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apify — Detalle sugeridos": {
      "main": [
        [
          {
            "node": "Vetting relevancia (Haiku)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Vetting relevancia (Haiku)": {
      "main": [
        [
          {
            "node": "IF — hay semillas TT",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF — hay propuestas": {
      "main": [
        [
          {
            "node": "POST Propuestos",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Cerrar run en el registro",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Armar propuestas": {
      "main": [
        [
          {
            "node": "IF — hay propuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF — hay semillas TT": {
      "main": [
        [
          {
            "node": "Apify — Lookalikes TikTok",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Armar propuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apify — Lookalikes TikTok": {
      "main": [
        [
          {
            "node": "Vetting TikTok (Haiku)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Vetting TikTok (Haiku)": {
      "main": [
        [
          {
            "node": "Armar propuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Leer plan (fachada)": {
      "main": [
        [
          {
            "node": "Leer Propuestos",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "POST Propuestos": {
      "main": [
        [
          {
            "node": "Preparar puente propuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Preparar puente propuestas": {
      "main": [
        [
          {
            "node": "POST Puente propuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "POST Puente propuestas": {
      "main": [
        [
          {
            "node": "Cerrar run en el registro",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Buscar ahora (webhook)": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "templateCredsSetupCompleted": true
  }
}
