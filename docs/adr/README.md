# ADRs — Decisiones estructurales del sistema

> Una decisión por archivo. El valor está en el **porqué** y en las **alternativas descartadas** —
> lo que nadie puede reconstruir solo en 3 meses. Formato corto, sin ceremonia. Tabla-resumen en
> [PLAN.md §3.1](../../PLAN.md).

| # | Decisión | Estado |
|---|---|---|
| [ADR-001](./ADR-001-motores-heterogeneos-contrato-comun.md) | Motores heterogéneos, contrato común | Aceptada |
| [ADR-002](./ADR-002-supabase-registro-central.md) | Supabase (Postgres) como registro central | Aceptada |
| [ADR-003](./ADR-003-multicliente-desde-dia-1.md) | Multi-cliente desde el día 1 | Aceptada |
| [ADR-004](./ADR-004-interfaz-jefe-dashboard-y-push.md) | Interfaz del jefe: dashboard + resumen push | Aceptada |
| [ADR-005](./ADR-005-hosting-n8n-managed-fase1.md) | Hosting n8n: managed fase 1, VPS fase 2 | Aceptada |
| [ADR-006](./ADR-006-plano-de-datos-sin-workflow-padre.md) | Pipeline central como plano de datos (sin workflow padre) | Aceptada |
| [ADR-007](./ADR-007-convergencia-gradual-motor-unico.md) | Convergencia gradual a motor de research único | Aceptada (como dirección) |
| [ADR-008](./ADR-008-airtable-cockpit-equipo-redes.md) | Airtable como cockpit del equipo de redes (revisa D4) | Aceptada |
| [ADR-009](./ADR-009-scripts-literales-y-aprendizaje-en-scoring.md) | Scripts literales (transcribir/traducir) y aprendizaje en el scoring (revisa ADR-008) | Aceptada |
| [ADR-010](./ADR-010-scoring-semantico-y-etapa-calidad.md) | Scoring semántico con LLM + etapa CALIDAD (revisa ADR-009; enmienda 2026-07-14: Voz = contexto, no criterio) | Aceptada |
| [ADR-011](./ADR-011-tabla-ajustes-knobs-no-code.md) | Tabla `Ajustes`: knobs del scoring editables por el equipo sin código | Aceptada |
| [ADR-012](./ADR-012-senal-de-aprendizaje-bi-eje.md) | Señal de aprendizaje bi-eje (por referente Y por keyword/tema) | Aceptada (reducida por ADR-019: queda solo la señal por referente) |
| [ADR-013](./ADR-013-atribucion-multiproyecto-fan-out.md) | Atribución multi-proyecto: fan-out de un video a cada proyecto que lo reclama (grado 1, MVP) | Aceptada (solo por referente desde ADR-019; emisión deduplicada por ADR-018) |
| [ADR-014](./ADR-014-outputs-historico-canonico-archivado.md) | `outputs` = histórico canónico; lo escribe solo el archivado (el motor reporta solo `runs`) | Aceptada |
| [ADR-015](./ADR-015-busqueda-solo-referente-retiro-keywords.md) | Búsqueda solo por referente; eje TikTok-keyword dormante por flag (IG-keyword retirado) | Aceptada (cerrada por ADR-019: el eje keyword se remueve del todo) |
| [ADR-016](./ADR-016-knobs-de-ejecucion-globales-y-tope-de-costo.md) | Knobs de ejecución globales (top_n, recencia, resultados por referente) + tope de costo | Aceptada (extendida por ADR-017: piso por cuenta; enmendada por ADR-024: N vuelve por proyecto) |
| [ADR-017](./ADR-017-reactivar-keyword-tiktok-y-toggles-de-eje.md) | Reactivar eje keyword TikTok como toggle + 3 toggles de eje + knob/cap por keyword + piso por cuenta | Aceptada (revertida por ADR-019 en lo keyword; toggles de referente y piso quedan) |
| [ADR-018](./ADR-018-un-candidato-por-video-dedup-salida.md) | Un Candidato por video: dedup de salida del fan-out (gana la copia con mejor relevancia) | Aceptada (enmienda ADR-013) |
| [ADR-019](./ADR-019-remocion-total-eje-keyword.md) | Remoción total del eje keyword: el motor descubre solo por referentes (enmienda ADR-015, revierte ADR-017, reduce ADR-012) | Aceptada |
| [ADR-020](./ADR-020-motor-descubrimiento-referentes.md) | Motor de descubrimiento de referentes: sugeridos de IG + vetting Haiku + aprobación del equipo (workflow aparte; completa ADR-019) | Aceptada |
| [ADR-021](./ADR-021-medicion-desempeno-embudo.md) | Medición de desempeño del embudo: tabla `Métricas` + descartes del gate visibles + histórico enriquecido (el archivado computa, Airtable proyecta; enmiendas 2026-07-13 top-K, 2026-07-14 diagnóstico+barridos, 2026-07-14 bis descubrimiento+costo+páginas, 2026-07-15 split en `Métricas Proyectos` + `Métricas Global`) | Aceptada |
| [ADR-022](./ADR-022-loop-aprendizaje-criterios.md) | Loop de aprendizaje de criterios: destilación semanal en `criterios_aprendidos` + 🔥 prioriza ejemplos + salud por referente (extiende ADR-010, completa ADR-020) | Aceptada (se construye tras verificar ADR-021) |
| [ADR-023](./ADR-023-disparo-on-demand-boton-airtable.md) | Disparo on-demand: botón Airtable → automation → webhook n8n; señal desnuda, el motor lee Airtable; una corrida = todos los activos; coexiste con el cron | Aceptada |
| [ADR-024](./ADR-024-enmienda-adr016-n-por-proyecto.md) | Enmienda ADR-016: la N vuelve por proyecto (global = default), corte final por proyecto; `cap_top_n` intacto | Aceptada (enmendada 2026-07-17: spillover — N techo exacto, sobrantes al proyecto con cupo que también los gateó) |
| [ADR-025](./ADR-025-cockpit-producto-propio.md) | El cockpit migra a producto propio (frontend+backend+DB+auth sobre Supabase); Airtable queda como superficie interina curada al mínimo; B.2 retirado (cierra A.5, enmienda el invariante de ROADMAP §1) | Aceptada |
| [ADR-026](./ADR-026-stack-del-cockpit-propio.md) | Stack del cockpit propio: Next.js + Supabase Auth/RLS + Vercel, sin ORM, en este repo (`apps/dashboard/`) — implementa ADR-025 | Aceptada |
| [ADR-027](./ADR-027-postgres-fuente-unica-de-config.md) | Postgres es la fuente única de la config; Airtable se retira por dominio con corte seco por tabla (las tablas de Métricas mueren, no migran) | Aceptada |
| [ADR-028](./ADR-028-contrato-motor-run-plan.md) | El motor lee su config por `GET /api/engine/run-plan` (fachada versionada, fail-closed); el disparo sigue siendo señal desnuda | Aceptada |
| [ADR-029](./ADR-029-dedup-blindado-fail-closed-y-feed.md) | Dedup blindado: lectura de `processed_items` fail-closed, memoria grabada antes de entregar, `external_id` en el feed + `Leer feed vivo` como última línea (cubre el agujero de memoria de ADR-018) | Aceptada (enmendada 2026-07-28: los 3 lookups del dedup van `executeOnce` + retry ×3 y la lectura deja de filtrar por `in.(…)` — el fail-open que este ADR cerró se estaba disparando por timeout · **enmendada 2026-07-31: "la memoria antes de entregar" nunca estuvo en vigor** — el orden de ramas lo decide la posición en el canvas, no el array de conexiones; ahora va en serie. 🟠 pendiente de re-import · 🩸 **enmendada 2026-08-31: el fail-closed comparaba contra un techo que no existe y por eso NUNCA disparó.** La URL pedía `limit=50000` pero **PostgREST tiene `max-rows` en 1.000**: devolvía **1.000 filas de 1.547**, o sea que el motor llevaba meses **ciego al 35% de su propia memoria de dedup**, y el guard escrito justo para eso comparaba contra un número inalcanzable. La causa estaba escrita en el `notes` del nodo —*"la tabla es chica (~400 filas)"*, cierto al escribirlo— y **caducó sola porque `processed_items` no se barre nunca**. Ahora el nodo **pagina por offset** (1.000 × 50) y los guards son **dos**: el de 50.000 (que recién ahora ES el techo real) y **el de 1.000 exacto**, que es el que habría cazado esto. Probado en un workflow **desechable** antes de tocar el motor activo (1.547 = 1.547) y con un **contraejemplo de 999/1.001 que no abortan**. 🩸 **Y al medir el costo apareció algo peor que la ceguera:** en la corrida del 31/08 la línea **fail-closed** (`processed_items`) mató **0** videos y la **fail-open** (`Leer feed vivo`) mató **95** — y los 95 ciegos son **exactamente ese mismo conjunto**, así que la fuga real fue **CERO** y *la secundaria hacía el 100% del trabajo*. **Los roles que este ADR eligió a propósito se habían dado vuelta en silencio**: el único guardia era el que puede caerse sin abortar nada. ⚠️ Y **las dos se cortan en el mismo 1.000** (`candidatos` va por 274). El costo en plata **no está medido y no se afirma**) |
| [ADR-030](./ADR-030-descarte-duro-sin-transcript.md) | Descarte duro de los videos sin transcript en el gate (revierte la decisión #6, acota el invariante fail-open); retry en Supadata; `metricas.por_proyecto` con `tasa_gate`/`razon_faltante`; caps de entrega (cap_top_n 250, presupuesto 840) | Aceptada |
| [ADR-031](./ADR-031-transcriptor-a-pedido.md) | Transcriptor a pedido: el equipo pega enlaces y recibe el script literal; entran al dedup derivando el `external_id` de la URL (shortcode IG = id numérico en base64, 408/408) — sin tocar n8n. Cuarta zona `Transcribir`; lo que produce no es un Candidato (enmienda ADR-019 y el modelo de 3 zonas) | Aceptada |
| [ADR-032](./ADR-032-referente-proyecto-es-n-a-n.md) | Un referente alimenta N proyectos: la relación va en tabla puente (`app.referentes_proyectos`, migración `012`) y `app.referentes.proyecto_id` muere — el modelo de `009` truncaba 19 de 35 pares vivos y dejaba un proyecto activo sin fuentes. El contrato de la fachada no cambia de forma (sin re-import) | Aceptada |
| [ADR-033](./ADR-033-dueno-por-campo-durante-la-coexistencia.md) | Un dueño por **campo**, no por tabla, mientras n8n escriba en Airtable: `criterios_aprendidos`/`advertencia_criterios` siguen siendo de Airtable (su escritor es el archivado) aunque `Proyectos` ya sea de Postgres — si no, el loop de ADR-022 muere en silencio. Y la traducción de ids del contrato muere en **D7**, no en el corte 4/4: `typecast` convertiría un uuid en un proyecto fantasma | Aceptada |
| [ADR-034](./ADR-034-calificar-es-un-solo-acto.md) | Calificar un Candidato es **un solo acto**: 🔥/👍/👎, y el Estado se deriva (🔥/👍 ⇒ aprobado · 👎 ⇒ descartado). Los dos campos se siguen escribiendo con el mismo vocabulario, así que ninguna máquina se entera y D6 no obliga a re-importar. Cierra el 14% de aprobados sin emoji que le dejaba huecos al corpus de ADR-022; el precio es perder "buen video pero no lo quiero" | Aceptada (enmienda el glosario, no ADR-021/022) |
| [ADR-035](./ADR-035-contrato-de-escritura-por-postgrest.md) | La regla que gobierna D7: **n8n lee su config por la fachada, escribe sus resultados por PostgREST**. Airtable sale del sistema | Aceptada |
| [ADR-036](./ADR-036-los-descartes-no-se-barren.md) | `app.descartes` no se barre nunca y `falsos_negativos` pasa de proyección semanal a vista (`v_auditoria_descartes`): un descarte sin auditar antes del domingo era una auditoría perdida para siempre | Aceptada |
| [ADR-037](./ADR-037-miniaturas-por-proxy-propio.md) | Las miniaturas se sirven desde nuestro origen (`/api/miniatura`, allowlist de hosts) y se copian a Supabase Storage en la primera vista. La causa real no era el expiry: el CDN de Instagram manda `cross-origin-resource-policy: same-origin` y el browser bloquea el `<img>` cross-origin **siempre** | Aceptada |
| [ADR-038](./ADR-038-una-sola-perilla-de-cantidad.md) | Una sola perilla de cantidad: el `N` del proyecto, obligatorio; los 3 knobs globales pasan a `visibilidad = 'dev'` (**se esconden, no se borran** — borrarlos tiraría la recencia de 100 a 7 en silencio). Operar deja de decir «hasta» y muestra `pide N · X cuentas · la última entregó Y` | Aceptada (enmienda ADR-016 y ADR-024 en lo que ve el equipo; no toca el motor) |
| [ADR-039](./ADR-039-la-lista-resume-el-record-se-abre.md) | El estándar de interacción del cockpit: la lista resume, el record se abre en modal, crear es un botón arriba, y lo único que se edita desde la lista es el interruptor de prendido/apagado | Aceptada (convención de superficie sobre ADR-026) |
| [ADR-040](./ADR-040-los-criterios-de-la-voz-son-obligatorios.md) | Los criterios de la voz son obligatorios al crear **y al editar**, con `not null` en Postgres (migración `014`): se SUMAN a los del proyecto en el gate, así que una voz sin criterios juzga con la mitad del contexto, en verde y sin avisar. Para n8n es un aflojamiento del contrato, sin re-import | Aceptada |
| [ADR-041](./ADR-041-la-metadata-del-referente-es-derivada.md) | Los seguidores del referente se **derivan** en `v_salud_referentes` (último visto en `candidatos`, con fallback a `referentes_propuestos`), no se guardan en `app.referentes`: una columna copiada al aprobar se congela ese día y nadie la refresca — dato viejo con cara de fresco. 8 de 17 cuentas muestran «—», y es lo honesto | Aceptada |
| [ADR-042](./ADR-042-el-techo-de-gasto-se-toca-desde-el-cockpit.md) | Se cambia una perilla por otra: muere `Candidatos por corrida` (inerte, y su descripción describía a otro knob) y nace `Videos a transcribir por corrida` = `cap_top_n`, el freno de gasto que era el único que no estaba en la pantalla. Ajustes separa el bloque dev. **Obliga a re-importar** | Aceptada (enmienda ADR-016, completa ADR-038) |
| [ADR-043](./ADR-043-el-techo-se-muestra-la-entrega-no-se-promete.md) | Se muestra el **techo de crudos** (`cuentas × resultados por cuenta`), que es una multiplicación, no un pronóstico — la decisión de no estimar la entrega sigue en pie. Va donde se toma la decisión (campo `N`, Operar, Entender), avisa y no bloquea, y nombra las dos palancas | Aceptada (superficie; no toca datos ni el motor) |
| [ADR-044](./ADR-044-todo-nodo-caro-tiene-presupuesto.md) | Todo nodo caro tiene presupuesto y la **concurrencia** es la palanca de throughput (el presupuesto no puede pasar de ~880 s: watchdog de 900 s). `Traducir` era serial y sin red — el modo de falla más caro del motor. La asimetría importa: su presupuesto **degrada**, el de `Transcribir` **quema** (el video ya está en `processed_items`) | Aceptada (enmienda ADR-030 y ADR-042; **obliga a re-importar**) · **enmendada el mismo día: el techo se queda en 250** — `cap_top_n` no era freno de gasto sino el que **raciona el supply**, y con 0 la corrida quema ~265 videos que no vuelven sin entregar ni uno más (el techo de entrega lo ponen los `N`) |
| [ADR-045](./ADR-045-se-borra-solo-lo-que-nunca-produjo-nada.md) | Un registro de config se borra **solo si nunca produjo nada**: el rechazo dice cuánta historia cuelga y ofrece apagar. Los referentes salen siempre (su historia se guarda por handle en texto, no por FK); voces y proyectos, solo limpios. Sin migración y sin re-import | Aceptada (superficie sobre ADR-039; no toca el motor) |
| [ADR-046](./ADR-046-el-cockpit-es-multi-tenant.md) | El cockpit también es multi-tenant: `client_id`/`instance_id` en el schema `app`, con **doble grano** (empresa para voces/proyectos/referentes/usuarios, instancia para knobs/candidatos/descartes/eventos) y `clients.parent_id` para que un sub-cliente sea una fila y no una migración. **Extiende ADR-003** al schema que no existía cuando se escribió: entonces el cockpit era Airtable y el aislamiento lo daba la herramienta. Repara los 5 uniques globales — el peor es mudo: el dedup de `processed_items` es global y le vacía el supply a la segunda empresa | Aceptada |
| [ADR-047](./ADR-047-aislamiento-en-dos-capas.md) | El aislamiento va en **dos capas**: Capa 1 = el tenant tipado y obligatorio que el compilador no deja olvidar (entra ya, no toca prod, la lista de trabajo la produce `typecheck`); Capa 2 = policies de RLS + el BFF deja de leer con `service_role`. **Disparador escrito:** la Capa 2 entra antes de que un segundo cliente real tenga usuarios en producción — no antes (revierte parte de `011`), no después. La Capa 1 no se salta ni con RLS puesto: la fachada y n8n no tienen sesión de usuario | Aceptada |
| [ADR-048](./ADR-048-run-plan-v2-motor-por-instancia.md) | `run-plan` sube a **`version: 2`** con `?instancia=<uuid>` obligatorio (ausente, mal formada o ajena ⇒ 400/403 y la corrida no arranca), nace `GET /api/engine/instancias`, muere `fields.uuid`, y **`<<INSTANCE_ID>>` deja de ser "una constante de la instancia"**: viene del payload. Un solo re-import coordinado, con el checklist de los **6** placeholders (los 2 últimos muerden a mitad de corrida) | Aceptada (extiende ADR-028 por su propia regla de versionado; enmienda `ingesta-registro.md`) |
| [ADR-049](./ADR-049-un-pipeline-sus-tablas.md) | Un pipeline, sus tablas: lo común es el **contrato** (`content_item`/`output`, el registro, la identidad), lo propio es la **tabla** (`candidatos_linkedin`, …). **El enum `app.plataforma` no se toca.** La regla: ¿el dato tiene sentido sin saber de qué pipeline vino? es común; ¿cambia de forma según el pipeline? es propio | Aceptada (enmienda `workflow-manifest.md`; ejecuta PLAN §F5) |
| [ADR-050](./ADR-050-dispatcher-una-ejecucion-por-instancia.md) | El dispatcher dispara **una ejecución por instancia**, no un loop de tenants adentro de una: el loop rompe el invariante #1 y choca con los 900 s de `N8N_RUNNERS_TASK_TIMEOUT` (que ya mató 3 corridas). Cada tenant conserva sus 840 s. **No es el "workflow padre" de ADR-006** — ese ADR lo autoriza explícitamente como C9, y si el dispatcher se cae los crons y el botón ▶ siguen disparando | Aceptada (activa C9 de PLAN §2.2) |
| [ADR-051](./ADR-051-el-acceso-es-membresia-explicita.md) | El acceso es una **membresía explícita** (`app.usuarios_clientes`, con el **rol adentro**), no una herencia: `clients.parent_id` pasa a ser linaje y deja de gobernar permisos. La agencia es un **flag** (`es_dueno`), que además la deja **fuera de toda superficie que liste personas**. Y la regla que arregla la mezcla: **la membresía decide a qué cockpits entrás, no qué filas ves adentro** — el filtro es siempre la empresa del cockpit abierto. Se descartó que el rol FUERA la empresa (3 roles nuevos por cliente) y la herencia por árbol (*"¿quién ve a Viera?"* dejaría de ser un select) | Aceptada (enmienda ADR-046; **activa el disparador de la Capa 2 de ADR-047**) |
| [ADR-052](./ADR-052-el-sponsor-externo-no-ve-el-costo-del-proveedor.md) | El `sponsor` no ve el bloque de costos de Entender, y el corte va **en el servidor**: su única zona era justo la que muestra `app.tarifas`, o sea lo que cuestan los proveedores de la agencia. Con clientes externos logueándose, esa pantalla publicaba el margen. Se descartó mostrarle el precio de venta —necesita una tarifa por cliente que todavía no existe— y una pantalla propia | Aceptada (superficie; no toca datos ni el motor) |
| [ADR-053](./ADR-053-el-repo-es-la-forma-el-live-es-el-estado.md) | **El repo es la forma, el live es el estado**: los workflows se parchean por la API de n8n (`core/scripts/n8n-sync.mjs`) en vez de re-importarse enteros. El live es la base (ya tiene credenciales, ids de Apify y settings de instancia) y se le aplican los `parameters` del repo, con los placeholders **aprendidos del propio live** — no mapeados en el `.env`, que sería una segunda verdad que se atrasa sola. Fail-closed: si un placeholder no se aprende, no hay `PUT`. Semántica del PUT medida contra la instancia: `settings` **mergea** (por eso `binaryMode`/`timezone`/`errorWorkflow` sobreviven), `nodes` **reemplaza**, y sobre un workflow activo el `webhookId` y el `path` quedan intactos. **§Enmienda (30/08):** el push también cubre **topología** — el re-import queda sólo para crear un workflow de cero; el bloqueo nunca fueron las credenciales sino que `cuerpoPut()` mandaba las conexiones del live, y el nodo habría llegado huérfano. **§Enmienda 2 (31/08):** el balde benigno del `diff` se llamaba *"defaults de n8n, **o cambios sin empujar**"* y esa `o` era un bug — la regla era estructural (`live ⊆ repo`), así que un campo agregado al repo y nunca empujado salía junto a `method`, **con el comando en verde**. Lo destapó `options.pagination` en `Leer feed vivo`, la misma clase de fallo mudo del cierre 125. Ahora lo benigno se decide por **clave + VALOR** contra una lista cerrada de 6 pares (medida contra los 5 workflows live: cubre los 24 campos que el live no guarda, y el diff sale idéntico) y **lo que no está en ella grita**, en un balde propio `sin-empujar` | Aceptada · **enmendada 2 veces** (sucede al `deploy.mjs` deprecado; no toca los workflows ni el schema; **toca `core/`**: `n8n-sync.mjs`) |
| [ADR-054](./ADR-054-cada-run-lleva-su-execution-id.md) | **Cada run graba el id de su ejecución de n8n** (`params.execution_id = $execution.id`) y el error handler cierra por ahí: `PATCH /runs?params->>execution_id=eq.<id>`. `instance_id` identifica al **tenant**, no a la **corrida** — con el dispatcher (una ejecución por instancia) y tres pipelines compartiendo instancia, buscar «algún run en_curso de este tenant» toca la fila equivocada o varias. Medido: `$execution.id` existe adentro del workflow y el Error Trigger recibe **ese mismo** id. Va en `params` y no en columna nueva porque `runs` no necesita índice y una columna ataría el arreglo a la cola de migraciones (la `017` espera el re-import; `018`/`019` ya están pedidas) — con disparador de graduación escrito. Se borra la rama «insertar run de fallo huérfano»: exigiría inventar un tenant y `runs.instance_id` es `not null` (Capa 1 de ADR-047) | Aceptada (enmienda `ingesta-registro.md`; cerrada en prod el 2026-08-03, verificada end-to-end) |

| [ADR-055](./ADR-055-linkedin-es-un-pipeline-de-este-repo.md) | **LinkedIn es el pipeline N+1 de este repo**, no un repo aparte: cierra `maquina-linkedin` ADR 001 §3 (*"la infraestructura queda sin decidir"*) e importa sus ADR 002 y 003. Un cockpit de LinkedIn es **una `instances` más**. La etapa 1 se **bifurca en dos carriles** —personal (el archivo propio de la voz) y copiable (**Pinterest + inglés**, que es lo que destraba el proyecto: el material que se rebrandea es visual y nunca nació en LinkedIn)— y `enriquecer` es `n/a` porque ya es texto. **4 reglas duras: R-1 (gancho sin línea en blanco) y R-2 (firma obligatoria) se imponen por código; R-3 y R-4 son placeholders de la voz.** Publica un humano. Se descartó el repo propio: duplicaría cockpit, login, membresías, dedup e histórico, y le daría al equipo dos logins para dos pipelines de la misma empresa | Aceptada (ejecuta PLAN §F5 y la Fase 5 del plan multi-tenant; **no toca `core/contracts/`**) |
| [ADR-056](./ADR-056-las-zonas-son-rol-interseccion-pipeline.md) | Las zonas visibles son **`rol ∩ pipeline`**, y el selector se parte en **equipo** y **pipeline**. Con un solo pipeline los dos atajos funcionaban y ninguno se veía; con dos, `transcribir` lleva a una pantalla sin tablas y un selector plano hace que **saltar de empresa y saltar de trabajo sean el mismo gesto**. El de equipo se dibuja solo si alcanza más de una — que es lo que hace que **nadie vea el nombre de una empresa ajena**. Un pipeline sin declarar no dibuja **ninguna** zona: falla ruidoso. Se descartó declarar las zonas en el `workflow.yaml` (describe el motor, no el cockpit) y derivarlas de la existencia de las tablas (una migración cambiaría el nav sin que nadie lo decida) | Aceptada (superficie sobre ADR-026; completa ADR-051 en lo que se dibuja) |
| [ADR-057](./ADR-057-el-sheet-historico-por-instancia-o-ninguno.md) | **El Sheet Histórico: por instancia, o ninguno.** `sheet_id`/`sheet_tab` son constantes del nodo `Config` mientras `instance_id` —la línea de al lado— sí lee el body: con un solo archivado sirviendo a todas las instancias, la segunda empresa appendea sus aprobados **al Sheet de Retia**. Las dos salidas son baratas y lo que las separa no es técnico: `outputs` ya es el histórico canónico (ADR-014) y `/curar/historicos` ya lo muestra entero, pero el onboarding le promete al equipo *"el archivo de lo ya elegido"* y el one-pager le promete al jefe un **descargable a Excel**, y eso el cockpit todavía no lo cubre. Se mató, con el export construido primero. Lo que lo inclinó no fue el ahorro sino **de quién es el dato**: el Sheet deja el histórico de cada empresa en un archivo de Google colgado de una cuenta personal, donde el aislamiento del cockpit no llega — parametrizarlo hacía eso 3 veces | **Aceptada — opción 2, en 2 pasos.** Paso 1 (el export CSV) ✅ en prod; paso 2 (sacar los nodos) va en el re-import de D8 |

| [ADR-058](./ADR-058-el-flip-de-la-capa-2.md) | **El flip de la Capa 2**, paso 2 de 2 de ADR-047, escrito **después de ejecutar** (`d8edea2`) y por dos sesiones en paralelo que llegaron al mismo diseño — señal de que la decisión estaba forzada por el código y de que escribirla antes ahorraba un día. Tres hechos la reencuadran: **el disparador de ADR-047 se había cruzado sin registro** (7 membresías en 3 empresas, la primera persona que alcanza dos, una voz de `30x`) — el segundo tenant no estaba dado de alta, estaba **en uso**; **el flip no era "una línea"**, porque la fachada comparte `scoped()` con el cockpit por dos saltos (`run-plan` → `lib/config.ts` → las 4 lecturas) y sin cookie da `42501` ⇒ 503 ⇒ el motor no arranca; y **ninguna pantalla puede probar que tomó efecto**, porque la Capa 1 filtra igual. De ahí: la autoridad (`origen`) viaja en el `TenantContext` y el compilador la exige — elegir mal entre dos funciones sueltas es silencioso justo en la dirección peligrosa; la ventana **se cierra haciendo el flip**, no suspendiendo cockpits (enmienda la regla de ADR-047, escrita cuando retrasar costaba cero); y `lib/tenant.ts` se queda en `service_role`, así que el selector sigue con Capa 1 sola. Medido: con cuenta **no dueña**, 3 de 4 voces sin filtro de tenant — con cuenta dueña los números coinciden **por diseño** y no prueban nada | Aceptada (enmienda ADR-047; **no toca `core/`** — la `021` ya estaba aplicada) |

| [ADR-059](./ADR-059-lo-que-no-se-usa-no-existe.md) | **Lo que no se usa no existe.** La balde 2 que D7 apartó sin listar resultó ser **5 vistas y 12 columnas**, no "4 y 6" — y casi nada era huérfano: 4 de las 5 vistas tenían dueño escrito (ADR-019 §4 *descartó por escrito esta misma migración*, ADR-009 la tenía "en pausa", ROADMAP §C3 las usaba de criterio) y 2 columnas estaban en `ingesta-registro.md`. Manda el **consumo de código**: los docs se enmiendan, no al revés. Dos excepciones de frente: **`clients.parent_id` se queda** (ADR-051 §4 le dio trabajo nuevo hace 3 días) y **`runs.costo_estimado` se va con su línea del contrato** (el costo se calcula —`metricas × tarifas`—, no se guarda). Va en **dos migraciones porque las columnas se parten por quién las escribe, no por si tienen dato**: PostgREST rechaza el insert entero con `PGRST204`, y los dos POST que las mandan son `onError: continue` ⇒ el 400 **se traga** y deja al motor cerrando en verde sin memoria de dedup (los 15 duplicados del 20→21/07) y al archivado **borrando calificados sin archivarlos**. El dedup no se toca: su clave es `(instance_id, platform, external_id)`. Y el método queda corregido — **un objeto también está vivo si lo cita el runbook de un contrato, y grep no lee runbooks** | Aceptada (enmienda ADR-019, ADR-009 y ROADMAP §C3; toca `core/`: `022`, `023` e `ingesta-registro.md`) |

| [ADR-060](./ADR-060-el-equipo-se-administra-desde-el-cockpit.md) | **El equipo se administra desde el cockpit, y la autoridad se parte en dos.** Se escribe **antes** de construir, que es la lección que ADR-058 dejó cara (el mismo flip dos veces en un día). Entran 3 personas de Retia ⇒ se cruza el disparador textual de ADR-051 y la deuda que la `021:216` dejó escrita (*"una pantalla de accesos necesitaría una policy nueva"*). Cuatro decisiones: **`ajustes` es la 5ª zona y la ven los tres roles** —lo que se gatea son sus pantallas, y esto **corrige al plan** por un número medido contra prod: **8 de los 18 knobs son del `equipo`**, así que la versión "zona solo para dev y sponsor" le sacaba a Majo y Jero perillas que usan, sin aviso; **el techo de roles** (`nadie otorga un rol que no tiene`, en una función testeable y no en un `<select>`), que no es higiene sino **lo que impide desarmar el gate de costos desde la UI**; **la empresa sale de `ctx.clientId`**, así que el modo de falla mudo de ADR-051 desaparece por construcción; y **la lectura va por RLS pero la escritura por `service_role`** — no por conveniencia: `inviteUserByEmail` es Admin API y crea una fila en `auth.users`, que **ninguna policy puede autorizar**; la autoridad la pone la Server Action. Cierra además el hallazgo del gate de costos: `app.tarifas` tenía `using (true)` para todo logueado ⇒ la Capa 2 no respaldaba lo que la UI escondía. Se descartó la opción simétrica (escribir con la sesión y poner el techo en un `with check`): la Admin API hace falta igual, y **dos autoridades para el mismo acto es peor que una** | Aceptada (enmienda ADR-051, ADR-052 y ADR-056; toca `core/`: la `025`) |

| [ADR-061](./ADR-061-el-cockpit-se-nombra-no-se-adivina.md) | **El cockpit se nombra, no se adivina.** Una server action de Next **no recibe los `params` de la ruta**, así que las ~30 del cockpit llamaban `exigirTenant(zona)` a secas y caían al default de `resolverContexto`: *"el primero que alcance"*. Correcto para la raíz `/`; **adivinanza en todo lo demás**, e invisible mientras hubo una sola instancia activa. El 03/08 entraron las 3 de LinkedIn, `leerInstancias()` ordena por `(client_id, slug)`, y la primera pasó a ser `30x/linkedin`: desde ese día **cada acción del cockpit de Retia leyó y escribió en el tenant de 30X**, sin un solo error. Medido: 175 candidatos, **0 calificados**, cero eventos `candidatos.calificar` en 3 días — y **3 de 8 usuarios afectados**, los 2 `es_dueno` y la cuenta con dos empresas, o sea todos menos el equipo de redes (que veía una sola instancia y por eso acertaba). El caso peor no llegó por casualidad: `ajustes/equipo` documentaba *"la empresa sale del cockpit abierto, no hay dónde equivocarse"* y **habría dado de alta a la gente de Retia en 30X**; se salvó porque A5 no estaba deployada. **Los dos segmentos pasan a ser obligatorios** — no un default fail-closed, porque eso es una excepción de runtime cuando alguien hace click, y acá tiene que ser **error de compilación** (es ADR-047 Capa 1 un escalón arriba: tsc listó los 25 call sites). No es un permiso: el cockpit viaja desde la URL y se valida contra `instanciasVisibles`. Se descartó leer el header `Next-Url` (arregla el bug sin dejar la regla en los tipos: la próxima acción vuelve a andar por magia) y una cookie con el cockpit abierto (**es del navegador, no de la pestaña** — dos cockpits abiertos se la pisan) | Aceptada (completa ADR-047; cierra por construcción el modo de falla de ADR-051; **no toca `core/`**) |

| [ADR-062](./ADR-062-el-transcriptor-deja-de-ser-un-callejon-sin-salida.md) | **El transcriptor deja de ser un callejón sin salida.** Lo que producía no iba a ningún lado: el guion se quedaba en `app.transcripciones` y no llegaba al CSV que lee el jefe, y un pedido que falla para siempre (video sin voz) no tenía salida — el botón de reintento funciona y **no puede ganar nunca** en ese caso. Cuatro piezas de una idea: **Histórico deja de significar "lo que el equipo aprobó"** (una transcripción a pedido nunca se calificó; el glosario lo decía) y pasa a ser *"todo guion que el equipo quiso guardar"*, con columna `ORIGEN` **al final** del CSV para no correr las 15 de ADR-057; **entra sola al quedar `listo`** (si el histórico es lo que el equipo quiso guardar, pegar el link ya es quererlo); **el transcriptor abre su propio `run`**, porque `outputs.run_id` es `not null` y aflojarlo haría que esas filas desaparecieran de toda vista que hace `join runs`, en silencio — de yapa su gasto en Supadata deja de ser invisible; y **estado `abandonado`, que NO es `descartado`**: en este dominio descartar es siempre juicio de mérito, y un video sin voz no es malo, es **insumo roto** (un `descartado` de candidatos alimenta el aprendizaje como clase negativa; esto no debe alimentar nada). 🩸 La ADR se escribió afirmando que `outputs.tipo` no tenía check duro, citando media frase de la `001` — la otra media dice *"cuando se selle, se agrega el check en 002"*, y la `002` lo selló. Lo cazó un sondeo contra prod sin escribir nada (23514 vs. 23503); la corrección quedó **dentro** de la ADR. Se descartó `run_id` nullable (fallo mudo) y unir las dos tablas en la app (PostgREST no pagina un UNION) | Aceptada (enmienda ADR-014 y ADR-031; toca `core/`: la `026`) |

| [ADR-063](./ADR-063-el-sponsor-es-el-jefe-del-equipo-no-el-que-mira.md) | **El sponsor es el jefe del equipo, no el que mira.** ADR-060 lo dejó en *"el jefe que mira"* (solo `entender` + `ajustes`) y **el modelo no sobrevivió al uso: medido el 06/08 había CERO sponsors en las 3 empresas**. El agujero era estructural — el operador no invita, el dev es de la agencia y no puede estar de guardia, y el sponsor **no podía usar la herramienta que administra**. Ahora opera (las 5 zonas) y gana las 8 perillas de `visibilidad: equipo`. **No hizo falta migración, y eso es una propiedad del diseño**: RLS es por membresía de **empresa, no por rol**, así que ya alcanzaba los datos y lo único que lo frenaba eran las guardias de la app. **Ve Actividad, no ve Costos**: son dos gates distintos que estaban confundidos en uno — el log de quién tocó qué es el trabajo de un jefe de equipo; `v_costos_semana` es el margen de la agencia y el sponsor es de la empresa cliente (ADR-060 §5 la llama así con esas palabras). Y **un eje nuevo que no existía**: hasta acá los gates preguntaban *qué rol otorgás* (`rolesQuePuedeOtorgar`), nunca *a quién se lo aplicás* — **un sponsor solo toca operadores**, en cambiar-rol Y en quitar-acceso (separarlos deja el techo decorativo), y va dentro del `where` porque esas escrituras usan `service_role`. De paso cierra el agujero que `permisos.ts` documentaba como aceptado: el último sponsor ya no puede quitarse el acceso a sí mismo | Aceptada (enmienda ADR-060, confirma ADR-052; **no toca `core/`**) |

| [ADR-064](./ADR-064-la-tanda-es-el-pegote-no-el-procesamiento.md) | **La tanda es el pegote, no el procesamiento.** Llegó como *"puramente visual"* (agrupar los enlaces pegados juntos, con título y colapsar) y **medir mostró que la mitad no lo es**: `leerTranscripciones` trae **las últimas 50 y punto**, con 110 filas en la base ⇒ la pantalla ya oculta más de la mitad sin avisar, y esa misma ventana ya dejó una fila fallada inalcanzable. 🩸 La opción barata era reusar el `run` que ADR-062 crea, y **es incorrecta**: ese run es el **procesamiento** (de a 64, corte a los 45 s), así que 100 enlaces dan 2-3 runs y un run puede tocar varias tandas — agruparía por los pedazos en que la máquina decidió trabajar, no por los pegotes de la persona. La tanda nace **cuando alguien aprieta el botón**, así que es entidad propia (`app.tandas` + `tanda_id`). Título opcional con default `"20 links · 7 ago 14:32"` (tamaño y momento: no colisiona y ordena natural) y **editable después**, porque un campo opcional en medio de pegar 50 links queda vacío casi siempre. La página carga **cabeceras y baja las filas al expandir** — mismo patrón que llevó el feed de ~405 KB a ~16 KB, y acá el `script` es el campo gordo: una tanda colapsada no necesita sus guiones. Las 110 existentes se backfillean a sus **9 tandas reales** (el `creado_en` las separa limpio: un pegote es un solo INSERT), así el caso `null` no queda vivo. Se descartó agrupar por `creado_en` sin tabla (funciona hoy, pero es coincidencia de implementación: dos personas pegando en el mismo segundo quedan fusionadas para siempre) **Construida el 07/08** ([`027`](../../core/schema/027_tandas.sql), `domain/tanda.ts`, `lib/tandas.ts`, pantalla), y construirla corrigió dos cosas del texto: **el default NO se guarda, se dibuja** (guardarlo congela una proyección de dos columnas que ya están en la fila — ADR-041 — y duplica el formato de fecha, de donde salió la hora corrida 5 h de Entender), y **`creada_por` se muestra**, lo que abrió una **excepción angosta a ADR-051 §3** (§5): `app.autores_de_tandas()` no lista personas, resuelve la firma de un trabajo que la sesión ya ve, así que un dueño aparece en su tanda y sigue sin aparecer en la pantalla de equipo. | Aceptada · **construida** (extiende ADR-031, apoya en ADR-062; toca `core/`: `027` con tabla, columna, vista, policy y una función) |

| [ADR-065](./ADR-065-la-puerta-se-abre-con-contrasena.md) | **La puerta se abre con contraseña, y el link queda de repuesto.** El magic link costaba un correo por entrada, y **dos correos cada vez que había que alternar entre dos cuentas** — que es lo que pedían las dos verificaciones humanas que llevaban días abiertas (A7 y el clic a la tanda de ADR-064). 🔑 **Medir cambió el diagnóstico:** la cookie dura **400 días** y el proxy refresca bien, así que el re-login no era una sesión venciendo sino **la cuenta B pisando la de A** (la sesión es una sola por dominio y perfil). Sin esa medición el arreglo natural habría sido alargar una sesión que ya duraba 400 días. `signInWithPassword` sobre las cuentas que ya existen —no se recrea a nadie—, con el link plegado como camino de dos casos reales: toda alta nueva empieza sin contraseña, y el que la olvida no tiene otro modo. **El largo se valida al ELEGIR, nunca al chequear**: reusar la constante en los dos lados es un bug con fecha de activación (el día que el mínimo suba, el que tenga una más corta queda afuera sin que nadie toque su cuenta). **La puerta no contesta quién existe**: `invalid_credentials` y `email_not_confirmed` colapsan en el mismo estado —el segundo no es un borde, las 9 altas nacieron de `inviteUserByEmail`— y devuelve un **estado y no un texto** porque el estado viaja en la URL, así que un mapeo a texto habría publicado la diferencia ahí igual. Autoservicio en `/mi-cuenta` **sin pedir la contraseña actual**: quien entró por link no tiene ninguna, y la sesión es la prueba. Se descartó **Google OAuth** (pedido de Mani: dos consolas ajenas + deja que cualquier cuenta de Google cree fila en `auth.users`), el **código de 6 dígitos** (sigue costando un correo) y un **script dev-only que mine links** (si la puerta es incómoda se arregla la puerta, no se le da una llave al dev). Costo asumido: superficie de contraseñas, mitigada por signup cerrado + rate limit nativo + requisitos en la base y en la app + alta por admin | Aceptada (enmienda ADR-026 en su mitad de auth; **no toca `core/`**) |

| [ADR-066](./ADR-066-un-cockpit-sin-motor-solo-muestra-lo-que-se-configura.md) | **Un cockpit sin motor solo muestra lo que se puede configurar, y el disparo se guarda por pipeline.** ADR-056 le declaró a LinkedIn 4 zonas **cuando no tenía ninguna pantalla propia**: la tabla decía qué zonas *iba a tener*. Mientras tanto un cockpit de LinkedIn entrando ahí **renderiza las de reels**. `entender` es el caso conocido (5 vistas de reels filtradas por su `instance_id` ⇒ ceros sin fallar, la familia de la `015`); 🔴 **`operar` es de otra especie: trae los TRES botones que disparan los workflows de reels** (`correrAhora`/`buscarAhora`/`archivarAhora`), guardados solo por `exigirTenant("operar")` — que autoriza **la zona**, y `operar` la declaraban los dos. Las tres mandan `{ instancia }` con el uuid del cockpit abierto, así que desde `30x/linkedin` el ▶ le pedía al **motor de reels** que corriera sobre un tenant ajeno, sin fallar. Vivo desde el 03/08 en los dos cockpits `active`; **medido antes de cerrarlo: cero `runs`, cero `processed_items`, cero `outputs`** ⇒ nadie llegó a apretarlo y no hay nada que reparar. **Regla 1:** un pipeline declara solo las zonas que puede dibujar (LinkedIn queda en `curar` + `ajustes`; `historicos` y `sugeridos` tampoco, y no por faltar de construir sino porque **no tienen escritor**). **Regla 2, y no es redundante:** el disparo se guarda por **pipeline**, no por zona — sacar la zona cierra la puerta hoy, pero el día que LinkedIn recupere `operar` con su motor propio la guardia de zona **vuelve a autorizar el POST al motor de reels**, en silencio. La zona contesta *"¿este cockpit tiene esta pantalla?"*; hay que preguntar *"¿es el dueño de esta máquina?"*. Se descartó solo poner la guarda (deja `entender` en ceros mudos), construir las dos pantallas ahora (vacías por diseño sin motor) y derivar el dueño del webhook (las URLs son env vars sueltas, no hay de qué derivarlo) | Aceptada (enmienda ADR-056, completa ADR-055; **no toca `core/`**) |

| [ADR-067](./ADR-067-el-perfil-de-voz-de-linkedin-es-una-capa-sobre-las-voces-de-la-empresa.md) | **El perfil de voz de LinkedIn es una capa sobre las voces de la empresa, y su interruptor no es `voces.activo`.** `app.voces_linkedin` existía desde la `020` y **nunca tuvo código**; sin su pantalla, *"el cockpit está listo para configurar"* no tenía sujeto. Cruza dos granos que no son negociables: el roster es de la **empresa** (lo comparten los dos pipelines) y cómo habla esa voz en LinkedIn es de la **instancia** (PK `(instance_id, voz_id)`) ⇒ el perfil es una **capa opcional**, se cruza en memoria y se escribe con `upsert` sobre esa PK. **Cero migración.** 📏 Y medir cambió el alcance: ADR 002 daba por inventariadas las voces de 30X y desconocidas las de Retia, y **en el sistema es al revés** — `app.voces` tiene 3 filas, las 3 de `retia`, y `30x`/`estadox` tienen **cero** siendo las dos cuyo cockpit está `active`. Peor: **no tienen cockpit de reels**, así que no hay ninguna otra pantalla desde donde darles de alta una voz ⇒ la pantalla puede **crear** la voz, y esa es su única escritura sobre `app.voces` (las altas no tocan nada existente). 🔴 **La regla en rojo: lo que activa una voz en LinkedIn es que exista su perfil, NUNCA `voces.activo`** — ese flag significa de facto *"corre en reels"*, así que leerlo escondería voces válidas y **escribirlo apagaría proyectos de reels en producción sin un solo error**. Por eso la pantalla no tiene interruptor y el alta fuerza `activo: false` (guarda, no default: en Retia una voz nacida activa entraría al plan del motor de reels sin que nadie lo pidiera). El dominio valida lo que `text[]` no puede: padding `HH:MM` (`"8:00"` y `"08:00"` son la misma hora y dos strings — sin padding, ordenar texto pone `"8:00"` después de `"17:30"`), vocabulario de días ordenado por la semana, al menos una franja (una voz sin ninguna **no se puede programar nunca**) y techo de 168 h. Se descartó voces propias de LinkedIn (rompe la PK y duplica el roster de Retia), un `activo` propio (una columna para lo que la existencia de la fila ya dice) y resolver el alta con un runbook de SQL (deja la pantalla vacía y sin salida). **Verificado contra prod sin escribir**: 23503 con la forma exacta de la app, PGRST204 con una columna inventada, 23514 con `espaciado: 9`, y la tabla en 0 filas | Aceptada (construye ADR-055 §4, importa el ADR 002 del repo de diseño; **no toca `core/`**) |

| [ADR-068](./ADR-068-el-pipeline-lo-dice-la-instancia-no-el-que-pregunta.md) | **El pipeline lo dice la instancia, no el que pregunta.** El `workflow.yaml` de LinkedIn declaraba `GET /run-plan?ambito=linkedin` y eso **daba 400** — `ambito` es `motor\|completo` y nada más. 🩸 **Pero el 400 era el síntoma barato y no protege de nada:** `leerRunPlanCrudo` no tenía rama por pipeline, así que **sacar el parámetro** —o copiar un nodo de reels, que no lo manda— devuelve **200 con el plan de REELS de su empresa**. 📏 Medido contra prod el 09/08: `30x/linkedin` y `estadox/linkedin` dan **200 con el plan de reels VACÍO** (las dos empresas tienen cero de todo) y `retia/linkedin` da 403 por estar en `draft` — **pero el día que se prenda, que es el paso 2 del handoff, la misma llamada devuelve 3 voces, 6 proyectos y 17 referentes de reels**. Y el vacío de hoy es el peor de los dos: una corrida que termina en verde sin entregar nada se lee como *"todavía no cargamos referentes"*. La familia de la `015` y de ADR-066. 📏 **Medido también lo contrario, para no exagerarlo: la ventana es LATENTE.** Los 3 workflows que consumen `run-plan` reciben el uuid del dispatcher, que tiene 2 crons y pide `instancias?workflow=<su pipeline>`; no hay workflow de LinkedIn ni cron suyo, y el ▶ lo cerró ADR-066 ⇒ **ningún caller automático puede pedir ese plan**. El orden (deploy antes del `UPDATE`) se respeta por **asimetría, no por urgencia**: invertirlo solo deja el arma cargada para el primero que sondee la fachada a mano, que es justo lo primero que hace quien se sienta a construir el motor. **Son dos ejes y se separan: QUÉ pipeline lo deriva la fachada de `instances.workflow_id` —nunca se pide—, y CUÁN filtrado (`?ambito`) lo sigue pidiendo quien llama.** Dejar que el llamante declare su pipeline habilita que **contradiga a la base** sobre algo que la base sabe, y ese desacuerdo devuelve **200**. Además `ambito` ya significa otra cosa que LinkedIn va a necesitar el día que tenga archivado. Todo plan gana `pipeline` (**aditivo**: `version` sigue en 2, reels no se entera, y es el único chequeo que el motor puede hacer contra un fallo cuyo síntoma es un documento correcto); una instancia de un pipeline sin plan da **400 fail-closed** con el pipeline nombrado. **El plan de LinkedIn no es el de reels con campos de menos:** 🔴 filtra las voces por **la existencia del perfil, jamás `voces.activo`** (ADR-067 — ese flag significa *"corre en reels"* y la pantalla las crea con `activo: false`, así que filtrar por él daría **cero voces en las 3 marcas**, en verde), manda `carril` **resuelto** (decide el umbral, ADR-055 §2, y duplicado en un code node se desincroniza mudo), `proyecto_id` como string y **no** como array de un elemento (esa forma es herencia de un *link* de Airtable), y **no trae `proyectos` ni `ajustes`** — la unidad de config es la voz, y `ajustes: []` sería la lista siempre vacía que se lee como *"todavía no lo configuraron"* (llega con la `028`). Se descartó `?ambito=linkedin`, un `?pipeline=` validado contra la instancia (pide un dato que ya tenemos y suma un placeholder que puede quedar sin resolver), meterlo en el `TenantContext` (viajaría por ~40 funciones que no lo necesitan) y un endpoint por pipeline. **Para reels, cero cambios: `n8n:diff` verde en los 5** | Aceptada · **construida** (extiende ADR-028 y ADR-048; prepara la fachada para el motor de ADR-055, que **sigue sin existir**; **toca `core/`**: `contracts/run-plan.md`) |

| [ADR-069](./ADR-069-grabado-es-un-estado-del-sistema.md) | **Grabado es un estado del sistema, no un dato que vive en la cabeza del equipo.** Sale de contrastar un reporte de Majo contra prod, y **la queja más ruidosa era falsa**: *"la herramienta saca repetidos"* → `processed_items` tiene **977 filas y 977 `external_id` distintos**, y los 12 eventos `transcribir.pegar` dan **`ya_estaban: 0` en los 12**; los 50 links del doc de Mile entraron por primera vez el 07/08. *"Hay links que no sirven"* → de los 64 que entregó la herramienta **1 está borrado**, y los otros 6 caídos están en la mitad de un doc **armado a mano** (medido: sus guiones puntúan 0.03–0.16 de similitud contra los 509 guardados, contra 0.99–1.00 de los que sí produjo la herramienta). **Pero adentro había un hecho verdadero:** el equipo grabó videos que después volvieron a aparecer en una lista, y el sistema **no tenía dónde anotarlo** — `outputs.estado` admite `publicado` desde la `001` y tiene **0 filas**; el ciclo se corta en la calificación. `revisarPegote` no tenía un bug: le faltaba un hecho que nadie le contó nunca. **Es columna y no valor de `estado`**, porque ahí `estado` es el ciclo del *trabajo de transcribir* y grabar es **ortogonal** (un `listo` puede estar grabado o no; pisarlo perdería el resultado ya pagado y haría mentir a `reclamarPendientes`, `reencolar` y `abandonar`). 🔑 **Va SOLO en `app.transcripciones` y eso no es un recorte**: el motor **no puede** proponer dos veces el mismo video —medido, **los 298 entregados tienen su marca en `processed_items`, 298/298**— así que en ese carril la repetición ya es imposible por construcción y la columna en `candidatos` habría nacido **sin escritor**, justo lo que la `023` acaba de podar. Se descartó reusar `outputs.estado = 'publicado'` (rung 1, y se cae midiendo: **`outputs.external_id` significa dos cosas distintas según el carril** —uuid del candidato en 93, record id de Airtable en 79, id del video en 128— así que no hay clave por video; y `leerAprobados` filtra `aprobado`, o sea que mover la fila **la borraría del histórico que lee el jefe**), un valor más en `transcripciones.estado`, y una tabla `app.grabados` (la forma normalizada es **de más**: una columna nullable resuelve lo mismo sin policy, sin grants y sin join). El aviso **gana precedencia sobre los otros tres** porque es el único que dice *no lo vuelvas a mandar*. **No repara lo ya ocurrido** —los dos videos que Majo marcó entraron por primera vez el 07/08— y su canario está escrito: si en un mes `grabado_en is not null` da 0, la decisión estaba equivocada | Aceptada · **construida** (extiende ADR-031 y ADR-062; toca `core/`: la [`028`](../../core/schema/028_grabado.sql), una columna) |
| [ADR-070](./ADR-070-la-marca-de-grabado-es-por-video.md) | **La marca de grabado es por VIDEO, no por carril.** Enmienda ADR-069 a los 2 días: la columna `grabado_en` en `app.transcripciones` solo alcanzaba a los videos que entraron por Transcribir, y **55 de los 183 guiones del histórico vienen del Feed**, o sea que no tenían dónde marcarse. La llave pasa a ser `(instance_id, plataforma, external_id)` en su propia tabla `app.grabados` — la misma de `processed_items`. *La lección: se contó el match y se concluyó sobre el contenido.* | Aceptada · **construida** (migración [`029`](../../core/schema/029_grabados.sql) + su contract, la [`033`](../../core/schema/033_grabado_en.sql), las dos aplicadas) |
| [ADR-071](./ADR-071-el-export-es-un-xlsx-de-verdad.md) | **El export es un `.xlsx` de verdad, no un CSV disfrazado.** ADR-063 había elegido CSV en UTF-16LE + TAB para que Excel lo abriera nativo, y funcionaba a costa de que ningún parser máquina lo leyera sin saber el truco. Se escribió `domain/xlsx.ts` a mano —cinco XML en un ZIP sin comprimir, sin dependencia— porque un `.xlsx` **declara su encoding adentro** y no hay separador que adivinar. Las 17 columnas no se mueven. `ponytail:` el techo está declarado: si algún día hace falta *leer* un xlsx, formato condicional o estilos, gana la librería | Aceptada · **construida** |
| [ADR-072](./ADR-072-el-video-es-la-unidad-una-llave-una-tarjeta.md) | **El video es la unidad: una llave, una tarjeta, y el cruce en memoria.** El mismo video se veía de tres formas y en dos era irreconocible (Transcribir mostraba **la url y nada más**). 📏 El diseño arrancó asumiendo *"la metadata ya está, es un join"* y **medido es falso**: 0 de 130 en Transcribir, 3 de 294 en los cargados a mano. 🩸 El primer cruce dio 129/130 y era un **falso positivo** — las transcripciones matcheaban consigo mismas porque `outputs` guarda **la url en el campo `titulo`**. La tarjeta **degrada sin mentir**: lo que no se sabe se dibuja como falta | Aceptada · **construida** (migración [`030`](../../core/schema/030_videos_meta.sql), `app.videos_meta`) |
| [ADR-073](./ADR-073-la-coleccion-es-una-bolsa-de-videos.md) | **La colección es una bolsa de videos, y es donde se paga la metadata.** Tres pedidos que parecían features distintas (limpiar los elegidos · bajar un documento · *"quedan scattered around"*) eran el mismo hueco: **faltaba el sustantivo**. Es membresía explícita y no vista guardada, y apunta a la **llave** y no a `app.candidatos` — por eso el barrido del archivado puede borrar el candidato y la colección sigue entera, **sin tocar n8n**. Agrupar es el momento en que se paga el scrape, y es **fail-open**: si Apify se cae, el video entra igual | Aceptada · **construida** (migración [`031`](../../core/schema/031_colecciones.sql); el FK compuesto hace *imposible* meter un video en la colección de otra empresa) |
| [ADR-074](./ADR-074-el-guion-limpio-es-un-artefacto-nuevo.md) | **El guion limpio es un artefacto nuevo; el crudo no se toca.** **Enmienda ADR-009 y ROADMAP §1.1**, que decían *"sin reescribir, sin embellecer, sin voz"* con el visto bueno del jefe. Lo pidió Majo, que hoy lo hace a mano guion por guion. El limpio vive en su propia tabla, es opcional y desechable; `app.candidatos.script` y `app.transcripciones.script` siguen intactos — por eso se puede enmendar el norte sin romperlo. 🩸 El contraejemplo llegó antes que el feature: un video de **dos voces** que la limpieza convirtió en monólogo. *Se veía mejor y era peor* | Aceptada · **construida** (migración [`032`](../../core/schema/032_guiones_limpios.sql) + `voces.perfil_limpieza`) |
| [ADR-075](./ADR-075-agrupar-es-aprobar.md) | **Agrupar es aprobar: meter un video a una colección lo califica 👍.** Tapa un hueco de ADR-073 que nadie había visto: `Leer Candidatos calificados` archiva `estado <> nuevo` y `Barrer candidatos sin calificar` **borra `estado = nuevo` a los 20 días**, así que un video sin calificar metido a una colección **pierde su guion crudo** en esa ventana (`leerCrudo` busca en `transcripciones → candidatos → outputs` y se queda sin dónde mirar). 👍 y **no** 🔥 porque el destilador de ADR-022 usa los 🔥 como ejemplos positivos: agrupar 40 videos para bajarlos en un Word le reescribiría el norte al motor. **Nunca pisa una calificación existente.** 📏 Medido el 21/08: `app.colecciones` tiene **0 filas**, o sea que el hueco es de diseño y todavía no perdió nada. *La bolsa sobrevive; lo que la bolsa muestra, no necesariamente* | Aceptada · **sin construir** (Fase 1 de [plan-modo-seleccion](../agents/plan-modo-seleccion.md); sin migración) |

| [ADR-076](./ADR-076-ordenar-es-una-vista-no-una-consulta.md) | **Ordenar es una vista, no una consulta.** Llegó como un pedido puntual de Majo (*ordenar una colección por likes*) y se generalizó porque el mismo hueco está en las 4 pantallas que dibujan `TarjetaVideo`, que además ya tenían **tres implementaciones sueltas de "filtrar"** sin conocerse. 📏 Medido el 26/08: la colección *"Test"* trae **57/57** con likes, views, seguidores, engagement y heat pese a que `app.videos_meta` tiene 5 filas ⇒ **no hay que comprarle nada a Apify**; y ninguno de los 4 lectores tiene un solo `limit` ⇒ todo ya está en memoria. 🔴 **Se resuelve en memoria y no en la query, y en Colecciones es lo único posible**: `likes` no es columna de `colecciones_videos`, sale de `fusionar()` — un `.order()` obligaría a re-implementar esa fusión en SQL, justo lo que ADR-072 §2 prohíbe. 🔴 **Los nulos van siempre al final, nunca como 0**: son 129 en el histórico y decir que tienen *cero likes* es la mentira que la tarjeta se niega a decir; el precedente ya se pagó (ordenar por heat dejaba esas 129 desempatando por uuid, *un orden sin significado*). 🔴 **Y la línea que no se cruza: el filtro que EDITA no es el que MIRA** — el chip de calificación se queda en la query porque es lo que sostiene *"una tarjeta calificada no se va del mazo"* (ADR-034), mientras idioma y plataforma son inmutables y viven en el cliente sin congelado. Los criterios se declaran **por pantalla** y **el default de cada una no cambia** (en Descartes eso protege el near-miss de ADR-021); ordenar **no aplana los grupos**. 🩸 El primer borrador de la tabla de criterios daba `engagement` en las 4 pantallas y `likes` en Descartes, y **las dos son falsas**: `domain/video.ts` no transporta `engagement`, y **`app.descartes` tiene 12 columnas y ninguna es una métrica** — sus únicos ejes son relevancia, título y fecha. *Se corrigió leyendo los tipos y la tabla, no re-leyendo el diseño.* Se descartó el `.order()` de PostgREST, los nulos como 0, aplanar al ordenar, la perilla de agrupar, el filtro por referente (alta cardinalidad, decisión de Mani) y montarlo en Transcribir (0 de 130 con título: sería un adorno) | Aceptada · **construida** (extiende ADR-072; **no toca `core/`**, sin migración, sin n8n) |

| [ADR-077](./ADR-077-el-env-es-la-segunda-fuente-de-los-placeholders.md) | **El `.env` es la segunda fuente de los placeholders, y el idioma desconocido se traduce.** Dos problemas que aparecieron juntos. 🩸 El nodo `Transcribir` cerraba con `|| 'es'`, y a esa rama **sólo se llega cuando `guessLang` ya descartó los cinco idiomas que conoce** — o sea justo cuando se sabe que NO es español. Como el gate de `Traducir` es `idioma !== 'es'`, eso **salteaba la traducción**: el equipo recibía el guion en japonés o alemán, sin un error en el log. Y rompía un invariante escrito — `lib/transcribir.ts` dice que la app tiene que producir el mismo script que el motor, y la app con `lang` vacío **sí** traducía (aunque después etiquetara `'es'` lo que acababa de traducir). Ahora el default es `'otro'`, el valor que `normLang` ya usa. 🔒 **El contraejemplo es parte de la decisión:** un transcript español sin `lang` lo sigue cazando `guessLang` antes del default, así que no se paga Haiku para convertir español en español — clavado como tercer test. 2️⃣ Arreglarlo destapó que **el nodo era imposible de empujar**: los placeholders se aprenden alineando el `jsCode` **entero** contra el live, editarlo rompe la alineación, y `<SUPADATA_API_KEY>` aparece **1 sola vez en todo el repo**. El nodo se volvía inempujable *exactamente cuando alguien lo quería cambiar*. El `.env` entra como **segunda** fuente (nunca pisa al live, sólo mira lo pendiente, avisa en amarillo cuáles y jamás el valor) y **fail-closed queda intacto**, medido: `n8n:test` da 15 ok · 0 fallidos y su caso de fail-closed sigue abortando. Se descartó traducir siempre (paga Haiku por español→español, contra ADR-009), un valor nuevo `'desconocido'`, editar a mano en n8n (es lo que ADR-053 eliminó), el re-import, y que el `.env` gane sobre el live. La causa de fondo sigue viva: **el aprendizaje no sobrevive a editar un `jsCode`** — esto le da salida, no la arregla | Aceptada · **construida** (extiende ADR-053, arregla ADR-031; **toca `core/`**: `n8n-sync.mjs`) |

| [ADR-078](./ADR-078-el-video-se-baja-al-disco-no-al-cockpit.md) | **El video se baja al disco de quien lo pide; el cockpit no lo guarda.** Salió del onboarding de Dani y llegó **dos veces con motivos distintos** —Mani *"por si lo desmontan (JP Vieira)"*, Majo *"descargar videos de gráficas en colección para editores"*— que resultaron ser **uno solo**: los videos con explicaciones visuales (los de trading) tienen un guion que **no se sostiene solo con el texto**, así que si el creador baja el post el editor queda con un script sin fundamento. *"Videos de gráficas"* estuvo un día sin interpretar y era la pieza que faltaba. 📏 Tres medidas del 29/08, y **cada una descarta un diseño**: (1) **`videoUrl` ya vuelve del actor que ya se paga** ⇒ ningún proveedor nuevo; (2) 🔴 **la firma vence en ~38 h** (la miniatura dura ~5 días) ⇒ **no se puede persistir**, se compra al bajar, **y por eso esto no necesita migración**; (3) **~33 MB por video** (32.981.910 bytes, un reel de 93 s) ⇒ una colección de 57 son **~1,9 GB**: no hay ZIP que entre en los 60 s de una función. Se baja **de a uno** con el modo selección que ya existe, por un **proxy en streaming** (`/api/video`) que NO existe por la razón del de miniaturas —los mp4 vienen `cross-origin`, medido— sino porque **el `download` de un `<a>` lo ignora el browser cross-origin** y el link directo abriría el video en una pestaña. El allowlist de SSRF sale de la route a `domain/cdn.ts` con tests, porque duplicado es el que se endurece en un lado y no en el otro. 🔴 **Y la consecuencia se dice en voz alta: esto NO es un respaldo del sistema** — un video que nadie bajó se pierde igual; lo que cambia son los pasos, no esa propiedad. Se descartó copiarlos a Storage (~1,9 GB por colección, decisión de costo y retención que nadie tomó), guardar `video_url` en `app.videos_meta` (una columna con un link muerto a las 38 h), el ZIP, la cola con job, el `fetch()`+blob (el CDN no manda `Access-Control-Allow-Origin`) y montarlo en las otras tres pantallas | Aceptada · **construida** (extiende ADR-072 y ADR-073; **no toca `core/`**, sin migración, sin n8n) |
| [ADR-079](./ADR-079-el-descubrimiento-obedece-a-la-voz.md) | **El descubrimiento obedece a la voz, igual que el motor.** Majo pidió *"poder delimitar para quién quiero que traiga"* y la auditoría encontró que **el selector ya existía y el buscador lo obedecía a medias**: `Voces.activo` gobierna el motor (`?ambito=motor` filtra las voces y `armarRunPlan` saltea sus proyectos) y **no** gobernaba el descubrimiento, que pide `?ambito=completo` y solo miraba `proyecto.activo`. 📏 **Medido el 29/08 y mordía:** 3 voces con **1 sola activa** y 6 proyectos todos activos ⇒ el motor atendía **2** y el buscador **6**, cuatro de ellos de voces apagadas — o sea que apagar una voz no frenaba nada y se pagaban Apify y Haiku proponiendo cuentas para proyectos que no corren. 🔑 **El reflejo obvio está mal y por eso se escribe:** cambiar el descubrimiento a `?ambito=motor` arreglaría el alcance **rompiendo el dedup**, porque ese ámbito también filtra los referentes por `activo` y el buscador necesita los **inactivos** para no re-proponer una cuenta que ya existe apagada. La regla se queda en el nodo, explícita. Como desde acá el alcance es **el mismo para las dos máquinas**, Operar pasa a tener **una sola card** con los dos botones juntos, y los números propios del buscador (cap de 8 semillas, tope de 10 propuestas, afinidad 0.6) viven en Sugeridos. Se descartó un flag `descubrir_activo` por voz (un segundo interruptor al lado de uno que ya no se entendía, y una migración) y un selector mandado en el webhook (más de lo que hace el motor, y dos fuentes de verdad sobre el alcance) | Aceptada · **construida** (enmienda ADR-020; **no toca `core/`**, sin migración) |
| [ADR-080](./ADR-080-la-limpieza-la-decide-el-video-no-quien-aprieta.md) | **La limpieza la decide el video, no quien aprieta el botón.** Limpiar aplica los **criterios de la casa** (7 reglas de la agencia, iguales para todos) más **cómo habla la voz** que va a grabar; los primeros ya se aplicaban siempre y el perfil se **suma**, nunca reemplaza. El problema era el **selector**: una sola voz para toda la tanda, elegida por quien aprieta. 📏 **Medido el 29/08 y con los dos modos de falla ya en juego:** de los 57 videos de la única colección, **26 se limpiaron *sin voz* cuando su voz sí tenía perfil cargado** — salieron neutros y nadie podía notarlo, porque la pantalla no decía con qué criterios había salido cada guion; y el feed tiene **209 candidatos de las 3 voces**, así que la primera colección mezclada iba a recibir la voz de una aplicada a los videos de la otra. 🔑 **El video sí sabe de quién es** (`candidatos.voz_id`, y `outputs.metadata.voz` para lo archivado: **57 de 57** resuelven), así que la decisión se le estaba pidiendo a una persona teniendo el dato en la fila. La voz sale de `leerLoQueSeSabe` —la misma fusión que pinta la grilla, para que *la voz con la que se limpia sea la que la tarjeta muestra*— y `criterios_hash` pasa a ser **por video**. Además los criterios de la casa **se muestran** (solo lectura) y cada guion dice con qué salió. Se descartó **atar la colección a una voz** (rompe lo que la colección es y no arregla el fallo que ya ocurrió) y **hacer editables los criterios** (aplazado: la decisión de que vivan en código sigue en pie, y nadie chocó contra ellos todavía). 🔧 **Enmendada el 30/08 — la deuda que dejó tiene mecanismo:** `estaAlDia` dejó de estar huérfana, cada guion viejo lo dice en su tarjeta y hay **botón propio con confirmación**, nunca dentro de *Limpiar*. Un viejo tiene **dos** estados, no uno: los que se limpiaron *con* una voz cuyo video ya no la deriva **no se rehacen** —saldrían neutros, o sea pagando por empeorarlos— y eso **se despeja de las huellas solas**, sin consultar voces. El botón alcanza **la colección**, y eso deja **2 de los 28 inalcanzables a propósito** (medido: 27 viejos + 1 degradaría sobre los 65; 26 + 0 dentro de la única colección) | Aceptada · **construida y enmendada** (enmienda ADR-074; **no toca `core/`**, sin migración) |
| [ADR-081](./ADR-081-el-candidato-sabe-de-que-corrida-salio.md) | **El candidato sabe de qué corrida salió.** Operar dice *"hace X · entregó N"* y el Feed no decía nada: el 30/08 hubo tres corridas en un día y saber cuál trajo qué **pidió cruzar `creado_en` contra `runs.inicio` a mano**. La alternativa barata era derivar la corrida por rango —cero migración, cero n8n— y **se midió antes de descartarla**. 📏 **Contra prod el 30/08, 168 candidatos vivos y 38 runs de motor: 100 caen en 1 ventana, 0 son ambiguos, y 68 (el 40%) no caen en ninguna.** Los 68 comparten `creado_en` **al microsegundo** (`2026-08-22T02:35:28.3151`): son el rescate manual del 22/08, cuando la corrida `f3fcf3e7` murió quemando 814 transcripciones y sus filas ya armadas se re-insertaron por PostgREST para no volver a pagarlas — **su `creado_en` es la hora del rescate, no la de la corrida**. 🔑 O sea que la derivación **no falla ruidosa: le diría *sin corrida* al 40% del feed y la corrida existe**, está en la tabla y es la que se cayó. Y los **0 ambiguos no la salvan**: son 0 porque el guard single-flight impide corridas solapadas, así que el modo de falla que se temía (el solape) es justo el que NO se materializó. 🩸 **Hallazgo de paso: `outputs.run_id` es el run del ARCHIVADO, no el del motor** — hoy la corrida que produjo el guion **se pierde para siempre al archivar**, y ninguna derivación puede rescatarla porque el candidato ya no está. Anotado, no resuelto acá. **Nullable** porque `Abrir run` es sumidero (invariante #1): un registro caído deja el candidato sin corrida, nunca tumba la entrega. **Sin backfill**: escribir un valor derivado en una columna de registro lo vuelve indistinguible de uno medido (el mecanismo que contaminó el canario de ADR-074), así que las viejas quedan en `null` y el barrido de 20 días lo cura solo; las 68 del rescate son la única excepción y va **comentada y opt-in**, porque atribuirlas es *recordar* y no *derivar*. En el Feed entra como **faceta** de ADR-076 (no como criterio de orden: una fecha de corrida no es una métrica del video) y como texto en la tarjeta, con **`sin corrida` dibujado** y no omitido. Se descartó la derivación por rango, el `not null`, el backfill automático, el embed cruzando esquemas de PostgREST y filtrar por corrida en la query. 🔧 **Enmendada el 31/08:** filtrar por corrida **no es** agruparlas —la faceta contesta *"mostrame solo lo de anoche"* y el toggle nuevo *"mostrame todo, separado por corrida"*, y comparar dos corridas exige verlas a la vez—, así que **conviven**. El agrupado **anida** (corrida afuera, proyecto adentro) y delega el nivel de adentro a `agrupar()` para no tener dos implementaciones del orden por heat. 🩸 Y obligó a mandar **el ISO además de la etiqueta**: la etiqueta es texto para humanos, así que ordenar por ella pone *"1 sep"* antes de *"31 ago"* y el feed queda mezclado sin que nada falle. El cajón *Sin corrida* **se dibuja y va último** — medido: **242 de 274 vivos (88%) no la tienen**, o sea que hoy es casi todo el feed | Aceptada · **construida y enmendada** (extiende ADR-076; **toca `core/`**: migración [`034`](../../core/schema/034_candidatos_run.sql) ✅ **aplicada y verificada por su efecto** el 31/08, y una línea en el motor ✅ **empujada al live**) |
| [ADR-082](./ADR-082-un-video-quemado-se-rescata-borrandole-la-memoria.md) | **Un video quemado se rescata borrándole la memoria, no reconstruyéndolo.** Majo reportó *"unas corridas trajeron muy pocos videos"*; medido, no eran unas corridas. 📏 **593 transcripciones vacías sobre 1.755 mandadas a Supadata en 29 corridas: el 34% de la cosecha histórica**, con las tres que ella trabajó al 40%, 64% y 58%. `POST processed_items` corre **antes** de `Transcribir` (ADR-029 §2), así que el video que se comió un `429` **ya está en la memoria del dedup**: vuelve sin transcript, el gate lo mata como `sin_guion` y ninguna corrida futura lo vuelve a mirar. 🩸 **Dos hallazgos cambiaron la forma del problema:** (1) *"las corridas que hizo Majo"* **no es una consulta que se pueda escribir** — `runs` no guarda quién dispara, y `app.eventos` no tiene ningún evento de correr el motor, así que la ventana salió de cuándo trabajó ella; (2) **un quemado por el `429` y un rechazo real del gate SE VEN IDÉNTICOS** (los dos son una fila sin candidato), y `runs.metricas` guarda el contador y nunca los ids ⇒ **cualquier diseño que prometa "rescatar solo los quemados" está mintiendo**. Se rescata **borrándole la fila de `processed_items` al huérfano** (ni candidato vivo, ni archivado, ni descarte) para que la corrida siguiente lo vea como nuevo: **cero código y cero Apify**, porque el scraper ya baja esos 50 por cuenta en cada corrida y el dedup los tira. 🔑 **La pieza que lo sostiene: el `external_id` de Instagram ES el shortcode en base64** — `processed_items` perdió su columna `url` en la `023`, pero `outputs` y `app.descartes` guardan la URL y no el id. Probado **242/242** contra prod, y el script **aborta si no da 100%**: sin ese cruce el borrado se llevaba la memoria de **111 videos ya resueltos** y volverían al feed para calificar lo ya calificado. 🔒 **La evidencia se guarda ANTES de borrar** (el borrado destruye su propia prueba) y el `DELETE` va por **PRIMARY KEY**, nunca por un filtro de fecha: con la PK lo peor que pasa es borrar de menos. ⏳ Viable **ahora y no la semana pasada**: recencia 150 días y 50 resultados por cuenta ⇒ la antigüedad ya no es el límite. Es un **supuesto no medido**, así que arranca acotado (337 de 1.029) con el criterio escrito antes de medir: >60% ⇒ soltar el resto · **<20% ⇒ esta decisión no alcanza**. Se descartó el modo rescate en el motor (rama nueva + re-comprarle la metadata a Apify video por video: el huérfano viene sin cuenta, sin vistas y sin miniatura), pegar en *Transcribir* (la `010` dice que un enlace pegado **no es un Candidato**), soltar los 1.029 de una (gasta ~460 rechazos sin aprender nada) y el filtro de fecha. ✅ **MEDIDO el 31/08:** volvieron **82 de 337 (24%)** y **28 de los 32 candidatos que entregó la corrida (88%) son rescatados** — *el 8% sobre los rescatados es el número engañoso; el 88% sobre la cosecha es el que contesta el reclamo*. El confundido se descartó leyendo la ejecución de n8n nodo por nodo (`processed_items` se escribe **después** del heat-score, así que un colectado-y-filtrado se vería igual que un no-colectado): entran **82** al scrape crudo y llegan **los mismos 82** a `Transcribir` ⇒ el 24% es tasa de colección pura. 🩸 **El criterio escrito antes acierta la acción y erra la causa:** la tasa no depende del tamaño de la tanda sino de **qué cuentas siguen listando el video** — **11 cuentas de 40 ponen el 94% del supply**, 24 ponen 1-3 cada una y solo 4 tocan el tope de 50 ⇒ **los 255 que no volvieron no están pendientes, están fuera del alcance**. 📌 Y el hallazgo lateral vale más que el rescate: esas 24 cuentas de cola (`tesco`, `virginradiouk`, `filmmakerzara`) entraron por el descubrimiento y nadie las podó — **explican el *"trae pocos videos"* mejor que la ráfaga** | Aceptada · **construida, aplicada y medida** (limpia lo que dejó ADR-030 §Enmienda; **no toca `core/`**, sin migración, sin n8n) |

| [ADR-083](./ADR-083-una-corrida-cuenta-lo-que-anoto-no-lo-que-hizo.md) | **Una corrida cuenta lo que anotó, no lo que hizo.** Operar tenía una línea por corrida (`estado · hace X · entregó N`) y Mani lo dijo así: *"si falló no te dice en qué nodo; si salió bien no te dice cómo descartó. Me toca meterme a revisar la ejecución de n8n a mano."* 📏 **La lectura obvia —*falta registrar más*— es falsa para las `ok` y verdadera para las `fallo`, y esa asimetría ordena todo el diseño.** Medido el 31/08: una corrida `ok` ya guarda el embudo entero, `por_proyecto` con el diagnóstico calculado y `por_referente` handle por handle — y la card dibujaba **un número** (`outputs`); mientras que **las 12 corridas fallidas tienen `metricas` en NULL, las 12**, porque `Resumen del run` es el último nodo y morir antes es no anotar nada ⇒ **el detalle de un fallo es fino por construcción, no por falta de pantalla**. 🔑 **La decisión que arregla el *"se ve muy de dev"* no es de redacción: `colectados` cuenta VIDEOS y `pretrim`/`gate` cuentan video × proyecto**, por eso 1.682 sale de 520 sin que nadie baje más videos. El **por-proyecto pasa a ser la vista principal** (es la única en unidades sanas, dedupeada por `external_id`) y el embudo global queda abajo **diciendo la unidad de cada paso**. Son **4 tabs y no 3**: el transcriptor también escribe `runs` y es el de peor ratio (24 corridas, **12 fallos**); cada máquina dibuja lo suyo sin plantilla común, y un paso no registrado **no aparece como cero**. El veredicto tiene **dos capas que no se mezclan**: la determinística sale de `razon_faltante` (un LLM no la mejora y sí puede contradecirla) y la de la IA va **a pedido y guardada en `metricas.veredicto_ia`** (jsonb ⇒ cero migración), **solo sobre corrida cerrada** porque guardar es un read-modify-write sobre el jsonb que escribe n8n al cerrar. 💰 ~US$ 0,009 por corrida, una vez en su vida. ⚠️ `fetch` sin SDK (el invariante de `limpiar.ts`; el primer intento instaló `@anthropic-ai/sdk` y se revirtió). 🔒 **El link a n8n existe solo para `dev`** y el gate vive en el SERVIDOR: redes no tiene cuenta en n8n y un link que pide un login que no tenés es peor que ninguno. Lo que alcanzó a hacer un fallo **se cuenta de las tablas** (`candidatos.run_id`, ADR-081), no se lee de `metricas`. Se descartó una zona nueva en el nav (las zonas son verbos), la plantilla común, generar el veredicto en cada visita y borrar el link en vez de gatearlo | Aceptada · **construida y verificada en el navegador** (extiende ADR-021 y ADR-054; **no toca `core/`**, sin migración, sin n8n) |

| [ADR-084](./ADR-084-la-memoria-guarda-lo-resuelto-no-lo-intentado.md) | **La memoria del dedup guarda lo que se resolvió, no lo que se intentó.** Mani puso la condición al pedir más volumen: *"transcribir es el más delicado y no puede ser el que descarta"*. Y lo era: `POST processed_items` colgaba de `Heat-score v1` y corría **antes** de `Transcribir`, o sea que marcaba *"ya visto"* un video **antes de que Supadata lo mirara** — el que se quedaba sin presupuesto o se comía una caída volvía sin transcript, el gate lo mataba como `sin_guion` (ADR-030) y no lo miraba ninguna corrida más. 📏 **La corrida del 26/08 perdió así 144 videos de 250 (58%)**, con el aviso *"posible caída de Supadata"* impreso y los 144 quemados igual; ADR-082 midió el acumulado en **593 de 1.755 en 29 corridas (34%)** y tuvo que escribir un script para desquemarlos a mano. 🔑 **El bit que faltaba: *"lo intenté"* y *"me contestaron"* eran el mismo** — el nodo ya distinguía `transcript-unavailable` (definitivo) de un `429` (transitorio) desde ADR-030 §Enmienda, pero esa distinción moría dentro del `for` de reintentos y la memoria se escribía río arriba, ciega a las dos. Ahora `Transcribir` marca `_tx_resuelta` y **`Preparar procesados` quema solo los definitivos**: el presupuesto pasa de **quemar a postergar**, que es lo que `cap_top_n` ya hacía. El registro se corre un lugar en la CADENA (`Transcribir → Preparar procesados → POST → Traducir`) y no a una rama hermana: **`auditar-workflows.mjs` rechazó la rama** porque `Resumen del run` lee `$('POST processed_items')` para decir `registro_dedup`, y como rama deja de ser ancestro suyo — la misma clase de bug que dejó el dedup de ADR-029 sin efecto 3 corridas. `Traducir` pasa a leer por nombre, el idioma que `Transcribir` ya usaba. 🔒 Y `Preparar procesados` **aborta ruidoso** si ningún item trae la bandera: un recableado silencioso deja al motor sin memoria y la corrida siguiente re-paga todo, en verde. ⚖️ **Lo que se pierde, dicho sin eufemismo:** la ventana entre pagarle a Supadata y anotarlo deja de ser cero (un salto de nodo). Se cambia una **pérdida permanente y medida** por una **re-compra improbable**. Se descartó agrandar el presupuesto (mueve el umbral, no el hecho), anotar recién al final (alarga la ventana a 13-27 min) y quemar los transitorios y rescatarlos con ADR-082 (cuesta una corrida y recupera el 24%) | Aceptada · **construida y probada** (es el *"ADR nuevo sobre compensar la memoria"* que ADR-030 §Enmienda nombró por adelantado; **no toca `core/`**, sin migración, sin schema) |
| [ADR-085](./ADR-085-un-trigger-resuelve-sus-tablas-contra-un-camino-fijo.md) | **Un trigger resuelve sus tablas contra un camino fijo, no contra el de quien lo dispara.** Los 2 avisos reales de `get_advisors` se arreglan (migración `035`) y queda escrito **por qué los otros 6 son falsos positivos**: un lint con ruido que nadie documenta termina en que tampoco se miran los verdaderos.  Aceptada · **construida y probada** (es el *"ADR nuevo sobre compensar la memoria"* que ADR-030 §Enmienda nombró por adelantado; **no toca `core/`**, sin migración, sin schema) | Aceptada · **migración `035` APLICADA y verificada** (01/09): avisos de 9 → 7 

| [ADR-086](./ADR-086-la-identidad-de-un-video-no-es-la-de-su-post.md) | **La identidad de un video no es la de su post.** Dani lo reportó: *"me están apareciendo en el feed videos que ya había calificado y que grabamos ayer"*. Tenía razón, y **no era ninguno de los bugs ya arreglados** (los 5 sospechosos se descartaron cada uno con su medición: 0 duplicados por `external_id`, 0 de sus 147 calificados volvió a `nuevo`, `ignore-duplicates` nunca pisa una nota, 0 candidatos vivos en `outputs`, `n8n:diff` verde). 🔑 **El dedup recuerda el ID DEL POST, no el video**: cuando un creador re-sube el mismo reel Instagram le da un pk nuevo, y el motor lo re-transcribe, lo re-paga y lo deja en el Feed como nuevo. 📏 Medido en prod el 01/09 con **dos señales independientes** (caption idéntico + guion casi idéntico): **17 pares** en `app.candidatos` y **18 más** en `app.descartes`; **11 estaban en el Feed sin calificar con el gemelo ya calificado**, **3 de esos 11 con el gemelo ya grabado ese mismo día**, y **4 pares se juzgaron dos veces — 2 con nota distinta** (🔥 vs 👍), que es lo que envenena a `Destilar criterios` (ADR-022). No es regresión: los `colectados` pasaron de 524 a 1.178 al sumar referentes, y a ~100 posts de profundidad es donde viven las re-subidas. **Se AVISA, no se bloquea**, y ese es el corazón: el caption exacto caza 7 de 17 y **se equivoca en la mitad** (los creadores repiten caption en una serie) ⇒ *un aviso con 50% de precisión cuesta una mirada; un bloqueo con 50% cuesta un video bueno que nadie vuelve a ver* — mismo criterio que ADR-029. **La huella entra como dato y no se calcula en el dominio**, para que cambiar de fuente (caption hoy, guion original de Supadata con la `036`) no sea una segunda implementación de la regla. Se descartó la **duración** como llave sola (gratis y pre-pago, pero su tasa de colisión **no se puede cuantificar porque el dato hoy se tira**) y el hash del guion **traducido** (caza 1 de 17: Haiku no es determinista) | Aceptada · **rung 1 construida, typecheck + 494 tests verdes**; los 11 del feed **corregidos en prod y verificado por efecto** (222 → 211 sin calificar, 0 repetidos adentro) · **migración `036` APLICADA y verificada con 4 señales** (01/09): las columnas contestan, `con_huella = 0` y `con_duracion = 0` ⇒ el sin-backfill es medido, el índice parcial existe, y PostgREST devuelve 200 y no `PGRST204`. **el motor ya las escribe** (`Armar candidato` + `Preparar candidatos` al live, `n8n:diff` verde en los 5, `test-nodos.mjs` en 172 checks). 🔑 La huella sale de `d.transcripcion`, el transcript ORIGINAL de Supadata, y **no hubo que tocar `Transcribir` ni `Traducir`**: `Traducir` hace `Object.assign({}, d, {script})` y el original sobrevive al lado del traducido. 🩸 Un bug que cazó el test y no producción: `normalize('NFD')` separa la tilde y mandar el resto a espacio partía las palabras (*"como estas"* → *"co mo esta s"*), o sea una llave que **no matchea nunca y se ve idéntica a "no había repetidos"**. 🔴 **Falta la MEDICIÓN**, y la hace la primera corrida de redes |
| [ADR-087](./ADR-087-la-memoria-recuerda-lo-que-se-entrego-no-lo-que-se-evaluo.md) | **La memoria recuerda lo que se ENTREGÓ, no lo que se evaluó.** Mani lo puso en una frase auditando por qué una corrida entregó 13 de 100: *"los descartados no deberían pasar al dedup, solo los que se postean al Feed"*. 📏 **Medido contra prod el 01/09: `processed_items` tiene 1.952 filas y solo 866 videos llegaron alguna vez al Feed ⇒ 1.401 (71,8%) se scrapearon, se transcribieron, se PAGARON, se marcaron *ya visto* para siempre, y nadie los vio.** 🔑 **La causa es de modelo: la memoria contesta *¿ya lo evalué?* cuando el dedup necesita *¿ya se lo mostré al equipo?*** — dos preguntas distintas con la misma llave. 🩸 **Y verificar la premisa corrigió el alcance:** los pisos duros, el heat-score y la recencia matan ANTES de transcribir, así que **no entran a la memoria y ya vuelven en cada corrida** (`Preparar procesados` cuelga de `Transcribir`, ADR-084). Los 1.401 son enteramente del gate, del `sin_guion` y del corte por N — lo que muere DESPUÉS de pagar. **Tres preguntas, tres memorias:** `app.transcripciones` contesta *¿ya pagué el ASR?* (el motor la escribe y la lee; hoy tiene 130 filas y **las 130 son del transcriptor manual, el motor escribió cero**), `processed_items` pasa a contestar solo *¿ya se lo mostré?*, y `estado='sin_transcript'` contesta *¿tiene audio?* para no re-preguntarle a un mudo. ⚖️ **El orden de los cambios ES la decisión:** ADR-084 §Alternativas rechazó mover la memoria río abajo porque *"alarga la ventana de re-compra"*, y **ese argumento se cae con la caché** — si la corrida muere entre transcribir y entregar, la próxima encuentra el transcript y no le paga a Supadata. *Una memoria se puede mover río abajo cuando lo que protegía ya está protegido por otra.* 🔒 `app.transcripciones` gana `origen` (`manual`|`motor`) y **las 5 lecturas del cockpit filtran `manual`**: sin eso, `leerFallidas` —que trae SIN LÍMITE los `fallo`/`sin_transcript` porque *"son pocas por definición"*— se llena de máquina (ADR-082 midió 34% de transcripciones vacías) y `leerSueltas`, un canario documentado, deja de significar lo que dice. 🧩 **La RPC `app.cache_transcripts` existe por una limitación de n8n, no por gusto**: los Code nodes no pueden usar credenciales, y un GET con ~350 ids da **414** (el mismo límite que hizo trocear de a 200 a la app) ⇒ los ids van en el BODY. Y `app.descartes` gana `external_id`: era la única superficie sin identidad, tanto que `rescatar-huerfanos.mjs` tenía que **decodificar el shortcode desde la URL** para reconstruirlo. **Prioridad: lo nuevo primero, lo que vuelve rellena** — los que vuelven ya pasaron el piso de 100k vistas y llevan semanas acumulando reproducciones, así que mezclados por heat desplazarían sistemáticamente al material recién publicado. Se descartó una tabla nueva solo para el motor (sería un **cuarto** lugar donde vive el guion de un video, la fragmentación que ADR-072 vino a resolver), borrar la memoria con un script como ADR-082 (cuesta una corrida y recupera el 24%), no quemar nada, y backfillear los 154 descartes | Propuesta · **construida y probada** (migración [`037`](../../core/schema/037_origen_transcripciones_y_descartes_id.sql) **pendiente de aplicar**; motor: 4 nodos nuevos y 2 movidos, `auditar-workflows.mjs` sin hallazgos, `test-nodos.mjs` en **193 checks**; app: typecheck + **494 tests** verdes). ⚠️ **La app no se puede deployar antes de la `037`** |
| [ADR-088](./ADR-088-el-gate-ordena-no-veta.md) | **El gate ordena, no veta.** Mani, viendo el embudo: *"llenar el pedido con los top N y ya, no descartar si no son suficientemente buenos según la herramienta"*. 📏 **Contrafactual medido sobre 12 corridas: 417 entregados → 649 con top-N, +232 (+56%)**, y en 8 de 12 habría entregado más. 🔑 **Tres argumentos, en orden de peso:** (1) **vetar no ahorra un centavo** — el gate corre DESPUÉS de `Transcribir` y `Traducir`, así que el video rechazado ya se pagó (~USD 0,014) y ya estaba quemado; (2) ~~**la señal que vetaba es la más débil que hay** (relevancia 0,218 vs `log(views)` 0,493)~~ — 🩸 **ESTE ARGUMENTO SE CAYÓ, ver §Enmienda 2**: era Pearson global con el confounder de proyecto adentro. Con **AUC estratificado**, dentro del proyecto `log(views)` da **0,407** y `seguidores` **0,311** (predicen RECHAZO) y dentro de la misma cuenta **todas las métricas se evaporan**; la única que sobrevive es `relevancia_score` (0,638, y subestimada). **Las métricas son señal de CUENTA, no de video**; (3) **el humano ya es el filtro y funciona** — 96 de 211 son 👎 (45,5%), o sea que el gate era un segundo filtro más caro y no auditable. ⏳ **Se habilita AHORA por ADR-087**: hasta ayer lo que quedaba afuera por cupo se quemaba igual, así que entregar top-N seguía perdiendo el resto; con la memoria arreglada, **vuelve gratis la próxima corrida**. `relevante:false` deja de descartar y pasa a ser señal; el `composite` (0,7·Haiku + 0,3·métrica) sigue ordenando; **`sin_guion` sigue vetando** (ADR-030). 🔧 Y **`MIN_REL` pasa a ser el único veto, con lo cual `Relevancia mínima` deja de ser un knob INERTE** — estaba en 0 sin filtrar nada mientras el descarte real lo decidía un booleano que ninguna perilla tocaba. 📊 **La auditoría mejora**: `app.descartes` guardaba 10 por corrida y tenía 152 de 154 sin auditar; ahora todo entra a `candidatos` con su score y la pregunta se contesta con un `select`, sin que nadie audite nada. ⚖️ **Contras sin maquillar:** la calidad promedio baja (los nuevos traen score 0,00–0,50), **el 👎 va a subir** y eso es esperado, y cuesta atención — hay 211 sin calificar de 422. **La métrica de éxito la fija ahora ADR-089** (`aprobados / N pedido`): este ADR decía *🔥/👍 absolutos por corrida*, que corregía *"cuántos entregó"* pero seguía **sin denominador**. ⚠️ **§Enmienda 1: el escalón disparaba SIEMPRE y ahora sólo si N quedó corto** — el corte de `Armar candidato` va en dos escalones, porque el gate **no sabe cuánto falta para N**. Con eso queda superseded *forzar que los bajo-umbral queden siempre abajo* en su parte de **elegibilidad** (sigue en pie para el **orden**), **marcarlos con una columna nueva** (sería mostrar como autoritativa una señal que predice el veredicto con 0,218 — primero se mide, el score ya se persiste), borrar el gate (el score es el 70% del orden) y retirar `app.descartes` (queda dormida y vuelve sola si alguien sube el knob) | Propuesta · **construida y probada** (`test-nodos.mjs` en **199 checks**, 7 nuevos; `auditar-workflows.mjs` sin hallazgos). **No toca `core/`, sin migración.** 🔴 Falta la medición: cruzar `relevancia_score < 0.55` contra `calificacion` en la primera corrida real |
| [ADR-089](./ADR-089-una-sola-metrica-aprobados-contra-lo-pedido.md) | **Una sola métrica: aprobados contra lo pedido, por proyecto.** Cada cambio venía con su número y ninguno decidía nada. Mani: *"que de los videos colectados se cumpla con lo que pide cada proyecto y que sean APROBADOS"*. El norte es `aprobados / N pedido`, y se descompone en **cobertura × precisión**. 📏 Línea base medida: el techo real es la corrida del 31/08 17:22 con **45% del pedido** (Ansiedad 90%, Depresión 60%), y en **14 de 21** proyecto × corrida el motor ni llena N ⇒ **el cuello es cobertura, no precisión**. Enmienda la métrica de éxito de ADR-088. |
| [ADR-090](./ADR-090-la-metrica-no-rankea-videos-desempata.md) | **La métrica no rankea videos, y ni siquiera alcanza para desempatar.** Mani: *"el heat es una fórmula poco confiable para vetear los videos"*. 📏 **AUC dentro del proyecto**: `relevancia_score` 0,717 · percentil métrico **0,407** · `seguidores` **0,311** (predicen RECHAZO), y **dentro de la misma cuenta todas las métricas se evaporan** ⇒ el composite queda en **0,523, moneda al aire**. 🔑 **Y la decisión es BINARIA**: `relevancia_score` toma **27 valores** con paso **0,01**, así que el 30% métrico hoy no rankea, **desempata** — y para que fuera desempate *estricto* haría falta `peso > 0,990`. No hay punto medio. ⇒ **`peso_relevancia` 0,7 → 1**, un valor en `Config` (knob del cockpit, reversible sin deploy). Segunda señal: en los 3 (corrida × proyecto) 100% calificados, a mitad de cupo relevancia captura **22 aprobados contra 19**, gana o empata 3 de 3. **No se toca `Heat-score v1`** (es pre-transcript: ahí la métrica es lo único que hay) ni el fail-open (sin juicio sigue ordenando por métrica). ⚠️ Los empates quedan en orden arbitrario **a propósito**: no hay información para ordenarlos y el desempate viejo apuntaba al revés. 🔴 **No ataca el cuello**: el orden solo muerde en 7 de 21 (ADR-089) | Aceptada · en el live (`n8n:diff` verde en los 5) · `test-nodos.mjs` **213 checks** |
| [ADR-091](./ADR-091-la-segunda-oportunidad-cross-proyecto.md) | **La segunda oportunidad: antes de irse, el video se le ofrece a los demás proyectos.** Mani: *"que se revisen los otros rechazados de otros proyectos para TODOS los proyectos antes de irse"*. `Asignar proyecto+voz` hace el fan-out **por referente**, así que un proyecto que no tiene esa cuenta en su lista **nunca ve el video** — 📏 medido: **~3,5 proyectos de 11**, o sea que dos tercios nunca lo miran. Después del corte y el spillover, los huérfanos se le ofrecen a los proyectos **con cupo que no los vieron y tienen rúbrica**. **Sólo Haiku**: el transcript ya está pagado y `Transcribir` dedupea por video ⇒ cero ASR extra. 🔑 **Vive DENTRO de `Armar candidato`** porque es el único lugar que sabe *quién quedó sin dueño* y *cuánto cupo queda*; un nodo aparte tendría que re-implementar el corte. El costo de eso: el nodo que decide la entrega hace una llamada paga ⇒ **fail-open duro** + presupuesto (120 s) + tope (1.500 pares). 🔒 No rompe *un video en UN solo proyecto* ni N como techo; ⚠️ **roza ADR-019**: no agrega eje de descubrimiento pero **desacopla *de quién vino* de *para quién sirve***. 📏 Se mide con `metricas.segunda_oportunidad` y, por candidato y sin migración, con el prefijo `[2da oportunidad]` en `relevancia_razon`. **Criterio escrito antes de mirar: si da 0 corrida tras corrida, no es la palanca.** Descartado el **fan-out completo** (3× pre-trim y gate, y acerca los presupuestos al fail-open) | Aceptada · en el live (`n8n:diff` verde) · `test-nodos.mjs` **236 checks** |
| [ADR-092](./ADR-092-el-heat-score-es-una-etiqueta-que-desempata.md) | **El heat-score es una etiqueta que desempata pasivo, no una voz que vota.** Mani: *"es una métrica suave que sirve sólo como por tenerla… no es la voz final"*. 🔑 **Corrige el razonamiento de ADR-090**, que dejó los empates arbitrarios porque buscó el desempate como un **peso** y demostró que haría falta `> 0,990`. La conclusión no se seguía: un orden **lexicográfico** (relevancia, y métrica sólo si empata) es desempate **estricto por construcción, sin peso**. No es cosmético: relevancia toma **27 valores** y los grupos empatados son de **5,42, hasta 14**. Y la métrica se **persiste como etiqueta** (migración `038`): hasta hoy no existía en ningún lado porque el gate pisa `heat_score`, y **no se puede recomputar** — es un percentil relativo al pool de SU corrida, se pierde con ella (mismo caso que `run_id` en ADR-081). ⚠️ **Migración ANTES del push de `Preparar candidatos`** o `PGRST204` tumba el lote entero. 🕳️ **Lo que NO se hizo: pasarle la métrica a Haiku como contexto** — cambia la distribución de scores y rompe la comparabilidad, y sobre todo **no se puede medir en la misma corrida que estrena ADR-091** | Aceptada · **migración `038` APLICADA** (Mani, 02/09) y verificada por efecto con 4 señales: columna `numeric` · **`con_prescore = 0` sobre 422 filas** (sin backfill medido) · `comment` puesto · **PostgREST la devuelve con 200, no `PGRST204`** · `n8n:diff` verde en los 5 |
| [ADR-093](./ADR-093-el-jurado-ve-la-metrica-como-contexto-debil.md) | **El jurado ve la métrica, como contexto débil.** La otra mitad del pedido de Mani (*"que haiku la conozca, no es la voz final"*), que ADR-092 había dejado anotada. Cada video viaja al gate con **`pop`**: su percentil de popularidad (0-100) dentro de su proyecto. 🔴 **El riesgo salía de nuestra propia medición**: dentro del proyecto más vistas y más seguidores predicen **RECHAZO** (0,407 y 0,311) y dentro de la misma cuenta la métrica se evapora ⇒ decirle *"tiene muchas vistas"* a secas sería **inyectar una señal anti-predictiva dentro de la única que funciona**. Por eso el prompt trae **el calibre escrito**: débil, de la cuenta, sólo para desempatar, y *"un off-topic con pop 99 sigue siendo off-topic"*. Va el **percentil y no las views crudas** (un absoluto no dice nada sin referencia y cambiaría de escala entre corridas). ⚠️ **Levanta el "el prompt NO cambia" de ADR-088**, cuyo motivo era real: mueve la distribución de scores ⇒ se **marca la época** con `metricas.gate_ve_metrica`, porque sin eso cualquier medición que cruce las dos mezcla escalas. Y **NO entró en la misma corrida que ADR-091**: dos cambios que se miden con la misma métrica no entran juntos. ⚠️ La perilla es de **experimento, no del cockpit**: vive sólo en `Config`, no está en `AJUSTE_MAP` ni en el CATALOGO, así que se apaga con push y no desde la app | Aceptada · en el live (`n8n:diff` verde en los 5) · `test-nodos.mjs` **251 checks** |
| [ADR-094](./ADR-094-una-corrida-que-muere-tiene-que-cerrarse-sola.md) | **Una corrida que se queda sin créditos tiene que cerrarse sola y decir por qué.** Mani: *"cuando se mueren por créditos de Supadata o créditos de Apify, igual se registren como una corrida fallida, se cierren y que indique que fue porque ya no hay más créditos"*. 📏 El 09/09 tres corridas (exec 169, 170, 171) duraron **22 s**, colectaron 0 y quedaron `en_curso`, bloqueando al equipo 60 min cada una — y n8n reportó las tres **`success`**. 🔑 **Dos causas encadenadas**: el cierre cuelga del carril de datos y **con 0 items n8n no ejecuta el nodo** (ni el `IF — hay videos nuevos`, que existe para esto y quedó detrás del vacío que debía detectar); y los nodos de Apify son **sumidero** por el invariante #1, así que un 403 se vuelve *"no había nada"* y ADR-054 nunca se entera. *El invariante es correcto para el registro y equivocado para la compra.* ⇒ **no se construye un cierre nuevo: se hace que el motor grite lo bastante para disparar el de ADR-054.** Tres capas: **(1)** `Cupo Apify (pre-flight)` pregunta antes de gastar — fail-open, y con margen de **2,5** porque una corrida costó **2,27** medidos; **(2)** `Normalizar IG`/`TT` pasan a lote y separan **`{error}` = nos rechazaron** de **`{}` = no había nada**, gritando sólo si son TODAS (una cuenta caída cuesta una cuenta); **(3)** `metricas.etapa` como miga de pan, la única capa que cubre el zombi de la corrida 166 — no enumera causas, registra avance. 🔇 `Normalizar TT` **no grita a propósito**: saber si IG trajo algo exigiría leer una rama HERMANA, el bug de ADR-029. 🩸 **Y el bug que sólo apareció al dispararlo en prod: n8n parte el mensaje por el ÚLTIMO `:` y guarda sólo lo de después** — el primer intento llegó al run como *"no se pago ni se perdio ningun video"*, con la causa borrada. Ningún mensaje de error puede llevar `:`; hay dos tests que lo fijan. Descartadas: `onError: stopWorkflow` en Apify (un referente flaky mataría la corrida), que el barrido diagnostique por la API de n8n (con 0 zombies corta la cadena y mata el workflow; con 2 abriría dos runs), y `alwaysOutputData` (el centinela cruza 6 nodos). Supadata **no tiene** pre-flight: `v1/account`, `v1/usage`, `v1/limits` y `v1/account/usage` dan **404** los cuatro (re-medido 09/09) | Aceptada · **verificada en prod con el cupo agotado** (exec 174 cerró sola en `fallo` en 1 s, con el número adentro, y gastó 0) · `n8n:diff` verde en los 5 · `test-nodos.mjs` **276 checks** |

| [ADR-095](./ADR-095-un-transcript-cortado-no-puede-pasar-por-completo.md) | **Un transcript cortado no puede pasar por "completo".** Del 11% de guiones del 09/09 que terminan a mitad de frase, se verificó que el corte **ya viene de Supadata** (re-pedir el original en inglés termina en el mismo punto). 🔑 **El hallazgo que descarta cualquier umbral por caracteres**: `DaTf9Wqxt8p` (95 ch, 54s, sano) y `Day8CXdBLwK` (105 ch, 45.8s, cortado) son gemelos por largo de texto — **sólo la cobertura en segundos los separa**. Se guarda `cobertura_seg`/`duracion_seg`/`modo` siempre (gratis, viene en la misma respuesta que ya se paga) y el veredicto se **deriva**, nunca se guarda: un booleano pediría backfill en cada cambio de umbral. No hay estado `parcial` nuevo — rompería `app.cache_transcripts` y las lecturas por estado. 📏 **Medido (Tarea 3, 09/09) sobre 583 filas: mediana 0.996, p10 0.958. Umbral elegido: `0.9`** — el histograma no marca quiebre entre 0.8 y 0.9 (561 arriba de 0.9, 10 en el medio, 12 abajo de 0.8); se elige por recall porque un falso positivo es inofensivo por construcción (`mejor()` sólo pisa si cubre más). Del reintento con `generate` sobre los 12 cortados: **4 recuperan entero, 7 no mejoran, 1 devolvió un `202` asíncrono** (ver ADR-096). 💡 Hipótesis de un corte por "hueco > 20s" que separaría los 4 de los 7, explícitamente **no implementada** por sobreajuste (n=12) | Propuesta · migración `039` sin backfill (verificada por sus tres ceros) · histograma medido |
| [ADR-096](./ADR-096-un-202-de-supadata-no-es-un-error-ni-un-transcript.md) | **Un 202 de Supadata no es un error ni un transcript.** Encontrado midiendo el reintento de ADR-095: el video más largo del lote cortado (`DbHEVZkP5uJ`, 550.6s) devolvió `HTTP 202 {"jobId":"..."}` — el modo asíncrono de Supadata para jobs largos. Es mudo en los dos consumidores: `202` es `2xx` así que no dispara ningún `throw`, y `content` no viene, así que el video queda indistinguible de "sin voz". El nodo del motor encima **agota los 4 reintentos** contra un job que nunca completa sincrónicamente ⇒ **5 créditos pagados por nada**. 📏 Alcance medido, sin inflarlo: **1 sola fila de 584** supera los 300s. **Decisión: se documenta y no se arregla ahora** — el polling de `jobId` cambia el contrato de sincrónico a asíncrono y toca a los dos consumidores, con sus propios presupuestos de tiempo; no se diseña a ciegas sobre un solo caso | Propuesta · **no implementada**, a propósito |

**Cuándo escribir un ADR nuevo:** cada vez que una decisión costaría caro revertir (stack, motor,
store, modelo de datos, límites de servicio). Copiá el formato de cualquiera de estos archivos.
